# Review: charting-method-note-taking draft-v3

Written by: `review-blog-post` skill (invoked by the editor at Stage 3b, iteration 3, ceiling pass).
Sources: `blog-ops/drafts/charting-method-note-taking/draft-v3.md`, `blog-ops/drafts/charting-method-note-taking/draft-v2.md` (regression diff), `blog-ops/drafts/charting-method-note-taking/review-v2.md` (prior verdict), `blog-ops/drafts/charting-method-note-taking/outline.md`, `blog-ops/drafts/charting-method-note-taking/facts.md`, `blog-ops/drafts/charting-method-note-taking/brief.md`, `blog-ops/drafts/charting-method-note-taking/research/serp.md`, `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md`, `${CLAUDE_PLUGIN_ROOT}/standards/blog-craft.md`, `${CLAUDE_PLUGIN_ROOT}/adapters/publish/frontmatter/wordpress.md`, `blog-ops/profile/voice.md`, `blog-ops/profile/authors.md`, `blog-ops/profile/audience.md`, `blog-ops/profile/blog.md`, `blog-ops/profile/product.md`, `blog-ops/profile/custom-instructions.md`, `blog-ops/config.yaml`.
Read by: editor (decides next action), blog-writer (consumed as `review_path` when `mode=revise`).

**Purpose:** a deliberately critical, checklist-driven audit of the draft against every objective rule in the outline + brand docs. The review skill is independent of the writer so it isn't biased by the same prompts that wrote the draft.

## Reviewed

- Draft: `blog-ops/drafts/charting-method-note-taking/draft-v3.md` (word count: `2,252` body prose, headings included, marker text excluded, `## FAQ` excluded; `2,138` excluding heading text; outline roll-up target: `2,060`)
- Outline: `blog-ops/drafts/charting-method-note-taking/outline.md` (status: `approved`)
- Author voice: `olga` (from brief.md)
- Modules active: `product` ON, `competitors` OFF (all competitor checks skipped as configured in `config.yaml`).
- Prior review: `review-v2.md`, verdict `request_revisions`, 0 critical / 1 major / 5 minor.

### Scope of this pass

`diff draft-v2.md draft-v3.md` returns exactly five changed lines: `127`, `133`, `145`, `153`, `160`. Every other byte of the draft is identical to draft-v2, which review-v2 cleared on §1, §2, §3, §4 and §5. Sections that did not change are re-verified by grep here (forbidden phrases, em-dash, en-dash, URLs, markers, H2 order, FAQ set, frontmatter, keyword count and per-section burstiness were all re-run against draft-v3 rather than carried forward on trust), but they are not re-litigated on judgment grounds.

### Verification of the `major` fix from draft-v2

| v2 issue | Status in draft-v3 | Evidence |
|---|---|---|
| **Major 1**, line 153's "Four of them happened before the session even started" contradicted the five-item list directly beneath it | **FIXED, exactly as prescribed** | Line 153 now reads: "Every bad chart I have made failed in one of five ways, and I have made all five. Ouch. **Most of them were decided before the session even started**, which was a humbling thing to notice." The false count is gone, and the verb moved from "happened" to "were decided", which is the load-bearing change: the claim is now about when the mistake was *decided*, not when it was *committed*. Counting against the list, mistake 1 (columns not named beforehand, whose own closing line is "If you could not name the categories beforehand, this was never a charting session"), mistake 3 (too many columns, a setup decision) and mistake 4 (charting material that does not chart, a pre-session judgment) are pre-session decisions = 3 of 5 = "most". Mistake 2 (full sentences, committed while writing) and mistake 5 (never reopening it, after the session) are correctly excluded. The claim the section makes is now one its own list supports. Both voice beats review-v2 said to preserve survived verbatim: "Ouch." and "and I have made all five". The five mistakes at lines 157–161 were not renumbered, reordered or reworded, apart from the separately-requested repunctuation of mistake 4. |

### Verification of the four `minor` fixes from draft-v2

| v2 issue | Status | Evidence |
|---|---|---|
| Line 133, "both of those observations" echoed line 131 and briefly inverted the section thesis | **FIXED, verbatim** | Line 133 now: "Three tidy paragraphs would have carried exactly the same information and **buried both of those insights mid-paragraph**." The noun now distinguishes line 129's two surfaced insights from line 131's two margin-bound details, and "buried mid-paragraph" names the thing prose does that a grid does not. Line 131 was not shortened or deleted, per instruction. |
| Line 127, `[VERIFY:]` scope no longer covered the two research-methods claims that moved into line 131, plus British spelling inside the marker | **FIXED, verbatim** | Line 127 now: `[VERIFY: the sample-size and main-limitation characterizations in this table, and the two research-methods details in the paragraph below it \| source: writer's general research-methods knowledge, no external source in facts.md]`. Scope covers line 131, `characterisations` → `characterizations`, ` \| source:` separator intact and the clause still non-trivial. |
| Line 145, "paper makes you commit" restated line 137 with nothing added | **FIXED, verbatim** | Line 145 now: "For charting specifically, the difference I notice is more mundane: **you cannot quietly redraw a column you already ruled in ink**." Distinct payload (the irreversibility of ink) rather than a restatement of line 137's "forces you to commit". The preceding hedge sentence and the `[EXTERNAL_LINK_NEEDED:]` marker are untouched. |
| Line 160, comma splice + "prep columns for charts" garden-path in mistake 4 | **FIXED, verbatim** | Line 160 now: "a lecture you can predict, and prep columns for, charts fine; a rambling seminar does not." The paired commas isolate the relative clause and the semicolon replaces the splice. Prose-only attribution held: grep confirms no e-student.org URL was introduced. |
| Item 6 (OPTIONAL), add one standalone question to H2 4's opener at line 83 | **SKIPPED, and the writer was right to skip it** | The suggested wording, "Where does the work actually go?", would have landed 43 lines after line 40's existing "So where does the real effort go? Not into the format. The discipline is entirely in choosing the columns." Adding it would have reintroduced near-verbatim repetition of the post's single most important sentence, which is precisely the failure mode item 2 of the same instruction block was fixing. The item was marked optional and "skip if it does not land naturally"; declining it is a correct editorial call, not an unapplied instruction. See §10. |

## Verdict

`approve`

Reasoning (1–3 sentences): the one `major` from review-v2 is genuinely resolved (line 153's claim is now true of its own list, and the fix preserved both voice beats), all four `minor` items landed verbatim, and every forcing condition in the verdict tree is clear on a fresh re-run against draft-v3, zero forbidden phrases, zero em-dashes, zero en-dashes, zero forbidden SERP-competitor URLs, zero unsourced numeric claims, zero off-shape markers, a well-formed and correctly scoped `| source:` clause, H2 order and FAQ set exact against the outline, and word count at `+9.3%` inside the clean ±10% band. The single optional item the writer declined was declined for a good reason that this review endorses, and the draft carries `0 critical / 0 major` issues, which is the approve gate. The two residual `minor` rows in §7 are documented, accepted shortfalls carried forward unchanged from review-v1 and review-v2, both of which explicitly instructed the writer NOT to fix them; neither blocks approve and neither should trigger a further writer pass.

**Iteration counter:** this is revision pass `3` of `max 2`. Escalation is NOT required: escalation applies only when iteration > 2 AND verdict = `request_revisions`. The verdict here is `approve`, so the editor should mark Stage 3b complete and route the draft forward to Stage 3d (link resolution for the two `[EXTERNAL_LINK_NEEDED:]` markers, human verification of the `[VERIFY:]` marker), not to another writer pass.

## 1. Voice + tone

| Check | Pass? | Notes |
|---|---|---|
| Matches `author_voice`: `olga` (§`blog-ops/profile/voice.md` + `blog-ops/profile/authors.md`) | pass | All three `voice.md` signatures survive the revision intact: the reader-directed rhetorical hook (line 137), the one-word rhythm beat ("Ouch.", line 153), and genuine self-deprecation (line 107, "born from the chart I ruined by wedging one homeless fact sideways across three cells"; line 153, "I have made all five... which was a humbling thing to notice"). `authors.md` prescribes "warm, first-person, encouraging, lightly self-deprecating; mixes punchy short sentences with longer explanatory ones"; the draft delivers a 1-word sentence and a 53-word sentence inside the same section. The line-145 rewrite ("you cannot quietly redraw a column you already ruled in ink") is more in-voice than the phrase it replaced, concrete and slightly wry rather than abstract. |
| First person throughout (`I`, `we`, `you`), no `one`, `users`, `stakeholders` | pass | Grep `\b(one could\|one should\|users are\|stakeholders)\b` on draft-v3: zero hits. "one" appears only as a numeral or pronoun ("one page", "one row", "one case", "one of five ways"). |
| Active voice (no "X was scanned" shapes) | pass | Grep `\bwas (scanned\|checked\|monitored\|built)\b`: zero hits. The two passive constructions are both deliberate and both prescribed by prior reviews: "were shared publicly by a note-taking app account" (line 147, review-v1's wording to strip an unsupported reach claim) and "were decided before the session even started" (line 153, review-v2's wording; the passive is what makes the claim true, since it is the decision that is pre-session, not the act). |
| Burstiness present, every H2 section has visible sentence-length variation | pass | Per-section sentence-length stddev recomputed on draft-v3: intro 9.0, H2 1 8.1, H2 2 34.7, H2 3 17.3, H2 4 10.7, H2 5 10.3, H2 6 10.8, H2 7 13.2, H2 8 6.2, CTA 7.8. All clear the stddev < 4 fail threshold with room; the flattest section (H2 8, 6.2) is 55% above the floor. Shortest body sentence is 1 word ("Ouch.", line 153); longest is the 115-word list-in-prose trigger sentence at line 54. The three touched sections moved only marginally (H2 4 10.2 → 10.7, H2 6 10.3 → 10.8, H2 7 unchanged). |
| Forbidden phrases count: `0` (list below); any hits = fail | pass | All 23 base phrases from `writing-standards.md` §Forbidden phrases grepped case-insensitively against draft-v3 individually; `voice.md` §Additional forbidden phrases states "None beyond the generic list in writing-standards.md". Zero hits. |
| Em-dash count: `0` (grep `—`); any hits = automatic §1 fail, each logged in §7 as `major` | pass | `grep -n '—' draft-v3.md` returns nothing. |
| En-dash context: every `–` hit is inside a numeric range (digits on both sides) | pass | `grep -n '–' draft-v3.md` returns nothing. Zero en-dashes at all, so the numeric-range test is vacuous. |
| Intro jargon check (§Insider-jargon translation + `audience.md` §Niche jargon translation) | pass | Intro (lines 15–21) is unchanged from draft-v2 and carries no SEO/marketing jargon (no `SERP`, `funnel`, `featured snippet`, `E-E-A-T`). Both in-niche aliases are translated on first use at line 29: "grid notes or the matrix method. Both names describe one page: a grid where the columns are the questions you are asking, and the rows are the things you are asking about." |
| Product code-literal grep (module: product) | pass | `product.md` defines no code constants, only reader-facing tool names. Zero backticked internal literals anywhere in the body. |
| Intro-hook over-anchoring | N/A | The hook is a recognition hook ("you have probably seen the charting method listed... and then never actually tried it"), not a time-bound event peg. Check skipped per §1.9. The post carries no date-anchored claim and reads unchanged in 12 months. |

Forbidden phrases found (grep output from `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md` lists):
- None found.

## 2. Structure

| Check | Pass? | Notes |
|---|---|---|
| Frontmatter block present and valid per the publish adapter's frontmatter template | pass | Re-verified against `adapters/publish/frontmatter/wordpress.md` field by field: `title` (52 chars, inside the 50–60 band, quoted), `date: 2026-08-03` (`YYYY-MM-DD`), `excerpt` (137 chars, ≤160, quoted), `tags` (4 entries, inside the 2–4 band), `authors: Olga Pak` (the display byline from `authors.md`, not the lowercase slug), `draft: true`. Correctly carries NO `cover`/`heroImage` field, per that template's §No cover/heroImage field, the featured image attaches as a `featured_media` ID. Frontmatter is byte-identical to draft-v2. |
| Tags drawn from the live WP taxonomy (`blog.md` §Tag taxonomy) | pass | `note-taking`, `note-taking method`, `students`, `productivity`. Four verbatim rows of the live taxonomy verified 2026-07-10, so zero invented tags and no remap at staging. |
| Title in frontmatter matches outline "Final title" exactly | pass | `Charting Method Note-Taking: How to Do It in 5 Steps`, character-for-character against `outline.md` §Final title. |
| Slug / meta description match outline | pass | Slug is carried by the draft directory (`charting-method-note-taking`, matching outline §Final slug); the `wordpress-rest` frontmatter has no slug field. `excerpt` matches outline §Final meta description verbatim, all 137 chars. |
| H2 order in body matches outline body-sections list exactly (no additions, no removals, no reorderings) | pass | 8 body H2s at lines 23, 42, 60, 81, 117, 135, 151, 163 in outline H2 1–8 order, plus the CTA H2 at 171 and `## FAQ` at 177. The five H3 steps under H2 4 (lines 87, 93, 99, 105, 111) match outline H3 4.1–4.5 in order and content. The only delta from the outline strings is title-casing, required by `writing-standards.md` §Formatting. Unchanged from v1 and v2. |
| Intro is 2–4 paragraphs, 1–3 sentences each (hook / expertise / internal-link cluster / preview) | pass | 4 paragraphs at lines 15, 17, 19, 21 with 2 / 1 / 2 / 1 sentences. Target keyword lands in sentence 2 of the hook. The E-E-A-T expertise statement is present at line 17 ("I write about productivity systems for a living and build small AI tools on the side"), matching outline P2. |
| Intro internal links ≤2, woven into a sentence | pass | Exactly 1 (`/note-taking-methods`, line 19), woven into the "if you are still weighing your options" clause rather than stacked as a see-also block. |
| No absolute cross-post links | pass | Grep `https?://(www\.)?olgapak\.com/`: zero hits. All six internal links are root-relative with no trailing slash, matching `route_prefix: /` + `trailing_slash: false` in `config.yaml`. |
| Every H2 has at least one rhythm marker (bullet list OR standalone question OR concrete number OR burstiness) | pass | H2 1 mini table + standalone question at 40; H2 2 5-bullet list; H2 3 4-bullet list; H2 4 five H3s + concrete numbers; H2 5 5-column table + question at 129; H2 6 questions at 137 and 141; H2 7 numbered list + the "Ouch." beat; H2 8 question at 165; CTA a concrete named tool. Every section clears on a strong arm (list, table or standalone question), not on burstiness alone. |
| FAQ section has `### <Q>` items matching outline FAQ set 1:1 | pass | 5 questions at lines 179, 183, 187, 191, 195, verbatim matches of the 5 rows in `outline.md` §FAQ block, in the same order. |
| JSON-LD FAQPage schema in frontmatter matches FAQ body questions 1:1 | N/A | `wordpress-rest` emits no JSON-LD at the adapter level (`frontmatter/wordpress.md` §FAQ: "FAQPage JSON-LD is a WP-plugin concern"). Sub-check skipped per §2.1. |
| Outro / CTA paragraph present, immediately before `## FAQ`, nothing after the FAQ | pass | `## Start With One Chart This Week` (lines 171–175) sits immediately before `## FAQ` (line 177). The file ends at the final FAQ answer, line 197, with no trailing CTA. Two short paragraphs, no feature re-list. |
| CTA links this blog's CTA target with an action anchor | pass | `[Try my free AI tools](/ai-tools)` at line 175, matching `blog.md` §Primary CTA hook verbatim. Action anchor, not a bare homepage link. |
| H2 8 contains no comparison table (pillar-cannibalisation guard) | pass | Zero table rows between lines 163 and 170. One positioning paragraph plus one honesty paragraph, exactly as outline H2 8 specifies. |

Structural deltas vs outline (if any):
- None. Headings, order, intro shape, FAQ set, CTA placement and image slot count all match `outline.md` exactly. The draft-v2 → draft-v3 revision touched five prose lines and moved no structural element; the H2/H3 line numbers are identical across both drafts.

## 3. Facts + sourcing

| Check | Pass? | Notes |
|---|---|---|
| Every numeric claim traces to a `facts.md` entry OR has `[VERIFY:]` marker | pass | Every number in the body is procedural instruction, not evidence: "three to five columns" (25, 95), "Two to six words per cell" (101), "Two rows, three columns" (38), "Eight columns on one page" (159), "five ways" / "all five" (153), "Five steps" (83). None asserts a research finding. The one unverifiable evidentiary block, the worked-example table at 121–125, carries the `[VERIFY:]` at line 127, whose scope now also covers line 131. The `79 slides` stat from `facts.md` §Statistics (marked ⚠️ "Preferred: skip it") remains correctly unused. |
| **Invented-evidence check (editor-flagged, priority 1)** | pass | Zero retention/memory percentages, zero "studies show", zero "research suggests" attached to any charting claim. Grep for `studies\|research suggest\|retention\|memor\|proven\|evidence` returns only the honesty beat itself (75–79), FAQ answer 5 (197), and one metaphorical "evidence" in the argument-structure sentence at line 73. §3.8 invented-number patterns (`\d+K to \d+K`, `\d+\+ ` adoption claims, migration ranges): zero hits. |
| **H2 3 states the retention claim is unsupported and names the structural benefit instead (outline-required)** | pass | Lines 75–79 are byte-identical across v1, v2 and v3 and still do exactly what the outline demands: "I went looking for the research behind that line while writing this, and there isn't any: every guide making the claim cites nothing at all, and I could not find a single study on charting specifically. So I am not going to repeat it." Two full revision passes and no hedge crept in. |
| **FAQ answer 5 agrees with H2 3 rather than hedging back** | pass | Line 197, byte-identical across all three drafts: "I could not find a single study supporting a memory benefit specific to charting... treat that claim as unproven. The gains you can count on are structural." Same position, same firmness as the body. |
| **Misattributed social quotes (editor-flagged, priority 3)** | pass | Both Reddit quotes survive with their framing clauses intact. Line 115: "One r/NoteTaking poster described the underlying trap **for note-taking in general**". Line 161: "As one person on r/NoteTaking described **their own systems**", inside a mistake the draft pre-labels "a note-taking problem long before it is a charting problem". Neither implies the commenter used or endorsed charting. The `@DrAminaYonis` X quote is still unused. The Goodnotes X post (line 147) claims only what `facts.md` §Named examples supports. |
| Competitor mentions framed as "best for [segment]" | N/A | `modules.competitors` OFF in `config.yaml`. Check skipped. |
| Competitor pricing / feature freshness + `[VERIFY:]` on competitor pricing | N/A | `modules.competitors` OFF. Both checks skipped. |
| Product mentions (count: `1`, `modules.product` ON) each earning their place | pass | Exactly ONE named product mention: "Text Summarizer" at line 175, in the closing CTA only. It passes the deletion rule, since the sentence describes condensing a chapter to surface repeating categories, which is the pre-charting step the whole post says is the hard part, so removing the name leaves a hole. The intro's "build small AI tools on the side" (line 17) names no feature, per outline P2. Zero mentions across H2 1–8. Far under the >8 stuffing threshold. |
| Own-product pricing claims (no price, tier, usage number) | pass | Grep `\$\d`, `free up to`, `per month`, `tier`: zero hits. "free AI tools" is the durable framing `blog-craft.md` §Own-product pricing claims asks for. |
| Soon-to-ship feature gap check | N/A | `brief.md` §Soon-to-ship features lists "None". |
| Internal links (count: `5` post links + 1 CTA link) placed per outline | pass | `/note-taking-methods` (19, intro pillar up-link), `/outlining-note-taking-method` (73, H2 3 where an argument beats a grid), `/best-notebooks-for-note-taking` (149, H2 6 on dot-grid pages), `/cornell-note-taking-method` + `/mind-mapping-note-taking-method` (167, H2 8). All five outline rows placed in their planned sections. Anchor text is near-exact-match and varied. |
| First internal link appears before the first external link | pass | First internal at line 19; first external at line 56. |
| Every internal blog link is root-relative `{route_prefix}<slug>` with no trailing slash | pass | Grep of all six link targets returns `/note-taking-methods`, `/outlining-note-taking-method`, `/best-notebooks-for-note-taking`, `/cornell-note-taking-method`, `/mind-mapping-note-taking-method`, `/ai-tools`. All `/slug` form, no domain, no trailing slash, matching `trailing_slash: false`. |
| **Forbidden external links to SERP competitors (editor-flagged, priority 2, never module-gated)** | pass | `grep -oE "https?://[^ )\"']+" draft-v3.md` returns exactly two URLs: `https://www.utc.edu/enrollment-management-and-student-affairs/center-for-academic-support-and-advisement/tips-for-academic-success/note-taking` (line 56) and `https://subjectguides.york.ac.uk/note-taking/charting` (line 71). Cross-referenced against `research/serp.md` §Selected results analyzed, whose seven URLs are sheridancollege.libguides.com, subjectguides.york.ac.uk, evernote.com, plaud.ai, utc.edu, meetjamie.ai, e-student.org. Two of the draft's links (york.ac.uk rank 2, utc.edu rank 8) DO appear in the forbidden set but are `authoritative_allowlist` per `blog-craft.md` §Authoritative-site allowlist (`.edu` literal enumeration; the academic-domain heuristic clause for `.ac.uk`), so the allowlist exception applies and no flag fires, exactly as `outline.md` §External link plan pre-argued. **evernote.com, plaud.ai, meetjamie.ai, e-student.org and sheridancollege.libguides.com appear ZERO times as URLs.** Each is instead quoted with the vague prose attribution the outline mandates: "One college study-skills guide" (54, Sheridan), "One note-taking guide" (64) and "one note-taking guide sums it up" (103, both Plaud), "one beginner's guide" (89, 160, e-student). The line-160 repunctuation did not introduce an e-student.org URL, which was the specific regression risk in that edit. |
| External links (count: `2`) each point to reputable sources; anchor text = literal claim | pass, with a documented shortfall | Anchors are the literal cited claims: "set up your paper by drawing columns and labeling appropriate headings in a table" (56) and "harder to work with for STEM subjects that require visual representations like graphs or formulas" (71). 2 rendered links is below `blog-craft.md` §External linking's 3–5 guidance, but the two remaining external rows are correctly held as `[EXTERNAL_LINK_NEEDED:]` markers for Stage 3d, and `outline.md` §Open questions records the two-link outcome as a deliberate accepted decision ("fabricating a citation to hit a link count would violate the post's own honesty angle"). **Not logged as an issue and not a reason to revise.** If Stage 3d resolves either marker, the post lands at 3 or 4 and the shortfall disappears. |
| Target keyword appears 2–8 times in body (actual count: `7`) | pass | Brief target keyword: `charting method note taking`. "charting method" appears 14 times across the whole file: 1 in the title, 7 in body prose (lines 15 ×2, 21, 23, 42, 75, 81), 6 in the FAQ. The full-string hyphenated form "Charting method note-taking" lands exactly once, in the intro at line 15. Inside the 2–8 body band, no stuffing. Unchanged from v1. |

Unsourced / suspicious claims:
- **None.** Every claim in the draft carries a `facts.md` row, a prose attribution, a `[VERIFY:]` marker or an `[EXTERNAL_LINK_NEEDED:]` marker. The one coverage gap review-v2 logged (line 131's two research-methods details sitting outside the line-127 marker's stated scope) is closed by the widened marker in draft-v3, so the Stage 3d human now has an explicit instruction to check both the table and the paragraph below it.

## 4. Markers + placeholders

| Marker | Count | Placement OK? |
|---|---|---|
| `[VERIFY:]` | `1` | Yes, and the scope defect from v2 is fixed. Line 127, immediately under the worked-example table it scopes. Shape is canonical; the ` \| source:` separator is present and the clause is non-trivial ("writer's general research-methods knowledge, no external source in facts.md"), the honest-disclaimer form `blog-craft.md` §`[VERIFY:]` source clause explicitly accepts. Scope now reads "the sample-size and main-limitation characterizations in this table, **and the two research-methods details in the paragraph below it**", which covers line 131. Matches `outline.md` §Open questions row 2. The source clause names no file path, so no Glob cross-reference applies. |
| `[EXTERNAL_LINK_NEEDED:]` | `2` | Yes, and both are CORRECT, not missing links. Line 115 (retrieval practice, backing Step 5) and line 145 (paper vs digital) are external link plan rows 4 and 3, deliberately routed to Stage 3d. Both carry a `suggested source type:` clause. Neither claim is asserted as fact around the marker: line 145 still hedges to "Someone will tell you handwriting beats screens for recall", honoring the outline's "do not assert a paper-beats-screens claim unsourced". The line-145 rewrite happened *after* the marker in the same line and left both the marker and the hedge untouched. |
| `[INTERNAL_LINK_NEEDED:]` | `0` | Yes. All five planned internal targets were confirmed and linked, so no slot needed a marker. |
| `[IMAGE:]` | `4` vs outline `4` slots | Yes, exact match, and none moved in this revision. Line 27 (remotion, chart anatomy, after H2 1 + 1 paragraph), line 85 (remotion, five-step flow, after H2 4 + 1 paragraph), line 139 (ai-prompt, paper vs tablet, after H2 6 + 1 paragraph), line 155 (ai-prompt, overstuffed chart, after H2 7 + 1 paragraph). **Zero slots in H2 5**, where the worked-example table carries the visual load, per outline. **Zero featured-image placeholder**, correct for `wordpress-rest` (featured image ships as a `featured_media` ID, not a body placeholder) and consistent with `custom-instructions.md` §Image style pinning the featured slot to `ai-prompt`. Concepts, types and filenames all match `outline.md` §Image placement plan. |

Marker-shape problems (e.g., `[Verify:]` lowercase v, extra spaces, rogue formats the Phase 4 grep will miss):
- None. Grep `\[Verify:|\[verify:|\[Image:|\[EXTERNAL_NEEDED:|TODO:|\[TBD\]` against draft-v3 returns zero hits. All three marker shapes in use are the exact literals the Phase 4 compile greps for.

Sub-checks run and cleared:
- **§4.2a featured-image hygiene:** clear. No `Suggested filename: featured.*` anywhere, and none of the four concepts duplicates the outline's featured concept (a hand-lettered hero over a ruled notebook page). The line-27 anatomy diagram is the nearest neighbor but is a `remotion` callout diagram, not the `ai-prompt` hero; see §10 for the production note.
- **§4.2b image-placement hygiene:** the line-85 placeholder is followed (after a blank line) by `### Step 1:`. That is the placement `outline.md` §Image placement plan explicitly specifies ("After H2 4 + 1 paragraph"), and the five-step flow image previews the H3 sequence it introduces. Not logged as an issue, same call as v1 and v2. The other three placeholders are each followed by body prose or a list, not a heading.
- **§4.2c table-redundant chart:** the line-27 `remotion` placeholder sits 6 lines from the mini table at 33–36. The rule's own exception applies and the outline pre-argued it: the diagram adds labeled callouts (what a row label is, what a column header is, what belongs in a cell) that a markdown table cannot render. The line-85 `remotion` placeholder is a process flow, not a data chart, and the nearest table is 36 lines away. Not logged as an issue.

## 5. Word count

- Draft: `2,252` words (body prose: intro at line 15 through the end of the CTA at line 175, headings included, `[IMAGE:]` / `[VERIFY:]` / `[EXTERNAL_LINK_NEEDED:]` marker text excluded, `## FAQ` block excluded). Excluding heading text: `2,138`. Excluding the three markdown tables as well: `2,006`. FAQ, counted separately and not gated: `354`.
- Outline target: `2,060` words (`outline.md` §Word count roll-up, "Total estimate (body prose, no FAQ)")
- Delta: `+9.3%` on the primary basis (`+3.8%` excluding heading text)
- Band: `within ±10% = pass, no issue logged`

The five edits were net `+7` words (draft-v2 measured `2,245` on the identical basis, `+9.0%`), so the draft stayed comfortably inside the clean band. No trim or expansion is requested.

Per-section deltas vs the outline's per-section breakdown (marker and heading text excluded, so directly comparable to the v1 and v2 tables):

| Section | Outline | v1 | v2 | v3 | v3 delta |
|---|---|---|---|---|---|
| Intro | 200 | 176 | 178 | 178 | -11% |
| H2 1 What the Charting Method Actually Is | 200 | 182 | 182 | 182 | -9% |
| H2 2 When the Charting Method Wins | 200 | 269 | 225 | 225 | +13% |
| H2 3 When Charting Flops | 230 | 264 | 264 | 264 | +15% |
| H2 4 How to Use the Charting Method in 5 Steps | 350 | 326 | 331 | 331 | -5% |
| H2 5 A Worked Example, Start to Finish | 250 | 286 | 281 | 280 | +12% |
| H2 6 Paper vs. Digital | 180 | 231 | 189 | 196 | +9% |
| H2 7 Five Mistakes That Ruin a Chart | 220 | 239 | 251 | 252 | +15% |
| H2 8 How Charting Fits Next to the Others | 130 | 137 | 146 | 146 | +12% |
| CTA | 100 | 84 | 84 | 84 | -16% |

No section exceeds +15% of its own budget. H2 6 grew 7 words (the line-145 rewrite traded a 4-word clause for an 11-word one) and remains the most improved section across the three drafts (+28% in v1 → +9% now). The CTA's -16% is deliberate and correct for a closing CTA, flagged as such since review-v2.

## 6. Humanization floor (per `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md`)

| Check | Pass? | Notes |
|---|---|---|
| No forbidden phrases (0 hits) | pass | Both lists grepped case-insensitively against draft-v3, zero hits. |
| Zero em-dashes (`—`, grep returns nothing) | pass | Zero. |
| En-dashes (`–`) only in numeric ranges | pass | Zero en-dashes at all. |
| First-person throughout | pass | `I` / `you` throughout; no `one` / `users` / `stakeholders` as third-person address. |
| Active voice | pass | No passive-marker hits. The two passives (147, 153) are both wordings prior reviews prescribed, and in both cases the passive carries meaning the active would lose. |
| ≥1 rhythm marker in every H2 | pass | All ten sections clear, each on a strong arm (bullet list, numbered list, table or standalone question) rather than on burstiness alone. |
| ≥1 bullet list per ~300 words | fail (accepted, documented, do not fix) | 3 list blocks (5 bullets at 48–52, 4 at 66–69, the numbered 5 at 157–161; 14 list items total) across ~2,138 words = 1 block per ~713 words. Three markdown tables and the five H3s under H2 4 do the same wall-breaking work, and paragraphs run 1–3 sentences throughout, so the reader never meets a wall of text. **review-v1 §9 explicitly instructed the writer NOT to add lists** ("Do not add lists to H2 5, its table is the break. Do not convert existing prose paragraphs into bullets in H2 4"), and review-v2 carried the same call forward. This is a deliberate, twice-documented shortfall, not a writer failure. Logged as `minor` in §7 for the record; does not block approve and must not trigger a writer pass. |
| ≥1 standalone-question paragraph per major section (author voice calls for it, per `blog-ops/profile/authors.md` + `voice.md` §Lexicon) | partial (accepted, documented, do not fix) | 5 standalone questions: lines 40, 129, 137, 141, 165. H2 6 carries two. Four of ten sections have none (H2 2, H2 3, H2 4, H2 7). Review-v2 asked for at most ONE more, in H2 4, and marked it OPTIONAL with "skip if it does not land naturally"; the writer skipped it correctly (see §10). Every section already clears the rhythm requirement on a stronger arm. Logged as `minor` in §7 with a do-not-churn note. |
| ≥1 concrete number or named example per major section | pass | H2 1 the meeting mini table; H2 2 five named scenarios + the UTC guide; H2 3 four named avoid-cases + University of York; H2 4 "three to five columns", "two to six words"; H2 5 the three-method table; H2 6 the nursing-student tablet notes; H2 7 "Eight columns on one page"; H2 8 Cornell / outlining / mind mapping named; CTA the Text Summarizer. |
| No em-dash overuse | pass | Zero occurrences, so zero per 300 words. |
| Visible sentence-length variation in every section | pass | Every section stddev ≥ 6.2; range runs from a 1-word beat ("Ouch.", line 153) to a 115-word list-in-prose sentence (line 54). |

## 7. Specific issues

Issues ranked by severity. `critical` = must fix before approve; `major` = fix if revising; `minor` = nit-pick.

| Section (H2) | Line | Severity | Issue | Fix instruction (verbatim for writer if revising) |
|---|---|---|---|---|
| Whole post | 48–52, 66–69, 157–161 | `minor` | Humanization-floor residual, carried unchanged from review-v1 and review-v2. 3 list blocks across ~2,138 body words = 1 per ~713, against the standard's 1-per-~300 floor. The three markdown tables (33–36, 121–125) and the five H3s under H2 4 do the same wall-breaking work, and no paragraph in the post runs longer than 3 sentences, so the reader-facing symptom the floor exists to prevent does not occur. **Accepted, do not fix.** review-v1 §9 explicitly instructed the writer not to add lists, and converting prose to bullets now would flatten the burstiness that §1 currently passes on. | **No action. Do not fix.** Recorded so the humanize-text pass at Stage 4 does not "discover" this and start bulleting prose paragraphs. If a future editor ever wants to close it, the only arguably safe place is H2 3's narrative/argument paragraph at line 73, and even there the prose is doing comparison work a list would break. |
| H2 2, H2 3, H2 4, H2 7 | 42–58, 60–79, 81–115, 151–161 | `minor` | Humanization-floor residual, carried from review-v2. Standalone-question paragraphs stand at 5 (lines 40, 129, 137, 141, 165); four of ten sections have none. `voice.md` §Lexicon names rhetorical opener questions as an Olga signature. **Accepted, do not fix.** The one addition review-v2 suggested (H2 4's opener, "Where does the work actually go?") would have echoed line 40's "So where does the real effort go?" 43 lines earlier, reintroducing exactly the kind of repetition the same instruction block was fixing at line 133. The writer's decision to skip the optional item was the right call and this review endorses it. | **No action. Do not fix.** Do NOT add questions to H2 2, H2 3, H2 4 or H2 7. H2 4's opener specifically must not take a "where does the work go" question, that phrasing is already spent at line 40 and is the post's strongest single line. H2 7 already opens with the "Ouch." beat and a second device would crowd it. |

Zero `critical`. Zero `major`.

## 8. What the draft does well

Three specific strengths the writer should preserve. Calling these out explicitly prevents later passes (humanize-text at Stage 4, or any hand edit before publish) from rewriting strong sections accidentally.

- **The honesty spine at lines 75–79 and FAQ answer 5 at line 197 is byte-identical across all three drafts.** "I went looking for the research behind that line while writing this, and there isn't any: every guide making the claim cites nothing at all, and I could not find a single study on charting specifically. So I am not going to repeat it." No "studies suggest" hedge crept in across two full revision passes that reworked adjacent sections, and the FAQ holds the same firm line as the body instead of quietly walking it back. This is the plan's entire differentiator against a SERP where the analyzed competitors assert the retention claim with no citation, and it has now survived the harder test of surviving revision. **Change nothing here, at any later stage.**
- **The worked example at lines 121–133 now teaches the method three times over, and line 131 is the reason.** The table obeys the post's own Step 3 rule (every one of the 12 cells is 4–6 words, down from a 19-word maximum in v1), line 129 reads the finished grid the way a student would, and line 131 ("Two details did not survive the squeeze... Both of those belong in the margin, not in a cell") turns the compression cost into a live demonstration of Step 4's escape hatch. Draft-v3's swap at line 133 ("insights" for "observations", "buried mid-paragraph" for "hidden inside it") is what finally lets the closing thesis land without colliding with 131. **Keep the line-131 paragraph and the line-133 wording exactly as they are; they are load-bearing for each other.**
- **The Olga voice signatures and the forbidden-source discipline both held through two revisions.** "born from the chart I ruined by wedging one homeless fact sideways across three cells" (107), "Ouch." (153), and "Do you ever start a chart in a notes app and spend the session rearranging columns instead of listening?" (137) are the three `voice.md` signatures the v1 review found missing, and none reads as a retrofit. Simultaneously, across five edits touching the two riskiest sections, the Reddit quotes kept their "for note-taking in general" (115) and "their own systems" (161) framing clauses, mistake 4 kept its prose-only attribution with no e-student.org URL, and the Goodnotes claim at 147 still asserts only what `facts.md` supports. **Do not touch those framing clauses, they are what keeps the quotes honest.**

## 9. Instructions for writer (only if verdict = `request_revisions`)

N/A. Verdict is `approve`; there is no revise pass. The writer is done with this post.

## 10. Reviewer notes (free-form)

- **The one-line fix at 153 is the whole story of this pass, and it landed correctly.** Review-v2's issue was arithmetic, not style: "Four of them happened before the session even started" was false of the list beneath it under every reading. Draft-v3 replaces it with "Most of them were decided before the session even started". The verb change is what makes it true. Counting explicitly, mistake 1 (columns not named beforehand, whose own last line is "If you could not name the categories beforehand, this was never a charting session"), mistake 3 (too many columns, a setup decision) and mistake 4 (charting material that does not chart, a pre-session judgment) are all decisions taken, or not taken, before the session. That is 3 of 5, which is "most". Mistake 2 (full sentences, committed while writing) and mistake 5 (never reopening the chart, after the session) are correctly outside the claim. A strict reader who counts only mistakes 3 and 4 would get 2 of 5, but the sentence says "decided", not "happened", and mistake 1's own text attributes the failure to the missing pre-session decision, so the reading the paragraph invites is the correct one. **No further edit is warranted, and none should be requested.**
- **Endorsing the skipped optional item.** Review-v2 §9 item 6 suggested adding "Where does the work actually go?" to H2 4's opener at line 83. Line 40 already reads "So where does the real effort go? Not into the format. The discipline is entirely in choosing the columns." The suggested addition would have been a near-verbatim echo of the post's single best line, 43 lines later, in a revision whose item 2 existed specifically to break a two-sentence echo. The writer skipping it is the correct editorial judgment, and the reviewer proposing it was the error. Marking it as an unapplied instruction would be wrong.
- **What is actually left on this post, and it is all downstream of the writer.** Two `[EXTERNAL_LINK_NEEDED:]` markers (lines 115, 145) go to Stage 3d for resolution or deletion; `outline.md` §Open questions already accepts a two-external-link outcome if neither resolves. One `[VERIFY:]` marker (line 127) needs a human to check the research-methods table AND the paragraph at line 131, which is exactly what the widened scope now tells them. Four `[IMAGE:]` slots go to Stage 4a. There is no structural, sourcing, linking, marker, keyword, voice or word-count work left.
- **British spelling now survives only in one place that never ships.** Reader-facing prose is uniformly US English (`labeled`, `recognizable`, `analyzing`, `behavior`, `generalize`, `math`, `characterizations`; `A4` neutralized to "one page" back in v2). The single remaining British spelling in the file is `centre` inside the line-115 `[EXTERNAL_LINK_NEEDED:]` suggested-source-type text, which resolves and disappears at Stage 3d. Ignore it.
- **Profile-doc gap, open since review-v1 and worth fixing outside this post:** neither `voice.md` nor `site-conventions.md` states a spelling locale, yet the published corpus is consistently US English. A one-line "US English" note in `voice.md` would stop this costing a review round on every future draft. This is the only process improvement this post surfaced that has not been acted on.
- **For the image planner at Stage 4a:** unchanged from v1 and v2. The featured concept from `outline.md` (a hand-lettered hero over a notebook page ruled into labeled columns) and the line-27 in-post slot (a `remotion` anatomy diagram of a ruled grid) are adjacent enough to render as near-duplicates. They pass the featured-image hygiene check as written (different type, different job) but should be visually differentiated at production, ideally by making line 27 unmistakably a diagram with leader lines and labels rather than a second illustrated notebook page.
- **Two things a later hand-edit might be tempted to "fix" and should leave alone:** the H2 1 mention of the University of York at line 38 is deliberately unlinked because the York link is spent at line 71, per the outline's "use it in whichever section reads better, once only"; and the CTA at 84 words is deliberately under its 100-word budget, which is the right direction for a closing CTA.
- **Ceiling-pass posture, stated plainly:** this verdict is not a concession to the iteration budget. Had the line-153 fix been cosmetic, or had any of the four minor edits broken something adjacent, this review would have returned `request_revisions` at iteration 3 and let the editor escalate. It returns `approve` because the draft carries zero critical and zero major issues on a fresh, independent run of every objective check in §1–§6, not because it ran out of passes.
