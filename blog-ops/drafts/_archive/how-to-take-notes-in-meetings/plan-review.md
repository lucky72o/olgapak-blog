# Plan Review: how to take notes in meetings

Written by: plan-reviewer agent (Stage 1c.5). Read by: blog-editor (applies fixes or advances).
Independent review of `blog-ops/drafts/how-to-take-notes-in-meetings/plan.md`. The reviewer did NOT write the plan.

## Verdict

approve

One-line rationale: all seven rubric criteria pass, the angle is genuinely absent from the six analysed SERP competitors (confirmed against `research/serp.md` §"Angle opportunities" and `research/reddit.md` §"Angle opportunities"), sourcing is disciplined, and the external-link table is clean of forbidden competitor URLs; one non-blocking title-length fix is noted below.

## Rubric check

| Criterion | Pass? | Note |
|---|---|---|
| Angle/structure matches observed search intent (`research/serp.md` §"Search intent") | yes | `serp.md` §"Search intent" calls the dominant intent informational how-to (6/7 results are instructional guides) with a secondary commercial-adjacent pull from the three SaaS pieces. The plan's numbered/template structure with an opinionated spine matches this exactly, and it correctly picks up the secondary pull in §6 (AI notetaker honesty) rather than ignoring it. **Non-blocking note:** the recommended title, "How to Take Notes in Meetings: 7 Steps That Stick," is 49 characters (verified by count); `blog-craft.md` §"Title rules" requires 50–60 characters strict. Candidate 2 (52 chars) is the only one of the three options actually in range, but the plan correctly rejects it for undercutting the post's own thesis. Suggested fix for the outline stage: `How to Take Notes in Meetings: 7 Steps That Actually Stick` (58 chars, keyword-first, keeps the numbered-steps promise, doesn't contradict the "you should miss most of the words" argument). |
| Every planned H2 traces to a sourced fact in `facts.md` | yes | §1/§2/§4 trace to the c3rbutt and Tronracer quotes (`facts.md` §Quotes); §5 traces to the 45→10 minute stat and the Interesting-Post4178 quote (`facts.md` §Statistics, §Quotes); §6 traces to the four handwriting citations and the AI-notetaker-policy-block facts (`facts.md` §"Handwriting vs. typing", §Statistics); §7 traces to the svenson_26 quote plus the Ben Cotton idea that `facts.md` explicitly flags as usable-in-Olga's-own-words (`facts.md` §"Quotes available but NOT recommended for direct use"). §3 ("Before the meeting") has no dedicated `facts.md` citation, but it's procedural setup advice (agenda check, template ready) rather than a factual claim, so this doesn't need one. |
| Every competitor claim is fresh (`Last verified` <=14 days from today) | N/A | `modules.competitors` is OFF for this blog per the dispatch context; the plan makes no competitor pricing/feature claims and none is required. |
| Zero forbidden SERP-competitor URLs in the external-link plan | yes | Checked all 4 rows in "External links planned" against the 6 URLs analysed in `serp.md` (medium.com, asana.com, smallsheds.garden, duckalignment.academy, slack.com, teamwork.com). Scientific American, SAGE (journals.sagepub.com), BBC Worklife, and the official Cornell page are all `auth_allowlist`/`primary_source` per `serp.md` §"Citations harvested from competitors" and none match a competitor URL. Each row also correctly routes around the competitor that originally surfaced the claim (per `facts.md` §"Handwriting vs. typing"). |
| Length target appropriate for the intent and H2 set | yes | 1,800–2,300 words sits inside the how_to matrix's 1,500–2,500 range (`blog-craft.md` §"Post type matrix") and is a deliberately justified undershoot of the ~2,500-word SaaS pieces, explicitly to avoid the "what are meeting notes" throat-clearing `serp.md` §"Avoid pitfalls" flags as a weakness of that SERP end. Workable for 7 content H2s + FAQ at 1–3 sentences/paragraph. |
| Open questions genuinely resolved or explicitly flagged | yes | Two questions resolved with stated reasoning (handwriting citation choice; whether the equity point belongs in a practical how-to). One left open with an explicit fallback: if §7 (running-the-meeting / always-the-note-taker) proves overloaded, split into two H2s rather than dropping either — exactly the "flagged, not silently dropped" behavior the rubric wants. |
| Keyword's dominant intent is actually covered | yes | Brief's four explicit promises are each given a dedicated section: the template (§2), the before/during/after routine (§3/§4/§5), the honest AI trade-off (§6), and the five-minute post-meeting pass made concrete rather than a throwaway line (§5, backed by the 45→10 minute stat and the three-step process from the LPT thread). |

## Specific issues (only if not `approve`)

None. See the non-blocking title-length note in row 1 above.

## Revision instruction (verbatim, for the editor)

