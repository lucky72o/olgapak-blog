# Review: screen-free-hobbies draft-v2

Written by: `review-blog-post` skill (invoked by the editor at Stage 3b).
Sources: `blog-ops/drafts/screen-free-hobbies/draft-v2.md`, `blog-ops/drafts/screen-free-hobbies/draft-v1.md` (diff basis), `blog-ops/drafts/screen-free-hobbies/review-v1.md` (my iteration-1 review, archived), `blog-ops/drafts/screen-free-hobbies/outline.md`, `blog-ops/drafts/screen-free-hobbies/facts.md`, `blog-ops/drafts/screen-free-hobbies/brief.md`, `blog-ops/drafts/screen-free-hobbies/research/serp.md`, `research/reddit.md`, `research/x.md`, `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md`, `${CLAUDE_PLUGIN_ROOT}/standards/blog-craft.md`, `blog-ops/profile/voice.md`, `blog-ops/profile/authors.md`, `blog-ops/profile/audience.md`, `blog-ops/profile/blog.md`, `blog-ops/profile/custom-instructions.md`, `blog-ops/profile/site-conventions.md`, `adapters/publish/frontmatter/wordpress.md`.
Read by: editor (decides next action), blog-writer (consumed as `review_path` when `mode=revise`).

**Purpose:** a deliberately critical, checklist-driven audit of the draft against every objective rule in the outline + brand docs. The review skill is independent of the writer so it isn't biased by the same prompts that wrote the draft.

**This is a targeted re-review.** Iteration 1 returned 0 critical, 0 major, 7 minor. The scope of this pass is: (a) did each of the seven §9 instructions land and actually resolve the issue, (b) did anything I praised in §8/§10 regress, (c) was issue 6 over-corrected. I re-ran every objective check anyway, because a re-review that only inspects the changed lines cannot certify the unchanged ones.

## Reviewed

- Draft: `blog-ops/drafts/screen-free-hobbies/draft-v2.md` (word count: `3,074` body prose on the strict basis excluding frontmatter, FAQ, headings and `[IMAGE:]` lines, `3,274` on the editor's inclusive basis; outline target: `3,260`)
- Outline: `blog-ops/drafts/screen-free-hobbies/outline.md` (status: `approved`)
- Author voice: `olga` (from brief.md)

**Diff verification.** `diff -u draft-v1.md draft-v2.md` returns 9 hunks containing exactly 12 changed or inserted passages: lines 32 (new), 42, 54, 57, 65 (new), 71, 75, 100, 113, 145 (new), 172, 184. That matches the writer's report exactly. No other line in the file moved. Frontmatter is byte-identical. All 51 entry names and all 51 cost-tier strings are byte-identical (verified by extracting `^- \*\*name\*\* (tier)` from both drafts and diffing: `TIERS IDENTICAL`). The writer's claim that nothing else was touched is true, and I verified it rather than accepting it.

## Verdict

`approve`

Reasoning (1-3 sentences): All seven §9 instructions landed, and each one resolves the issue it was written for rather than gesturing at it, including the two that were load-bearing (the Harvard sentence boundary at line 71 and the litter-picking learning-curve contradiction at line 54). Nothing regressed: the diff is provably the 12 hunks and nothing else, every strength named in §8 of review-v1 survives verbatim, and per-section sentence-length variation went up in both sections that received a new question rather than down. Issue 6 is correctly calibrated, not over-corrected: exactly two standalone question paragraphs shipped, in the two slots I named, leaving 6 of 10 body sections with no reader-directed question at all, so the failure mode I warned about did not occur and is not close.

**Iteration counter:** this is revision pass `2` of `max 2`. If verdict = `request_revisions` AND iteration > 2: the editor should escalate to the human, not dispatch another writer pass. Verdict is `approve`, so no escalation arises and no further writer pass is needed.

## 1. Voice + tone

| Check | Pass? | Notes |
|---|---|---|
| Matches `author_voice`: `olga` (§`blog-ops/profile/voice.md` + `blog-ops/profile/authors.md`) | Pass | Upgraded from "Pass with note" in review-v1. The register gap I logged (draft reading cooler and more essayistic than authors.md's "warm, encouraging, lightly self-deprecating") is closed by the new beat at line 32: "I am not good at most of these, for the record. I am just stubborn about the second session, and the second session is where a hobby actually starts." That is a direct hit on voice.md §Tone's documented evidence pattern ("Self-deprecating humor that keeps credibility") and it relocates the author's authority from hobby mastery to the mechanism the post is actually about, which is the strongest available version of the beat. First person throughout. The career-pivot anecdote still appears twice (16, 106) and still argues the thesis rather than flexing. |
| First person throughout (`I`, `we`, `you`), no `one`, `users`, `stakeholders` | Pass | Grep for `\b(one could\|one should\|users are\|stakeholders)\b`: zero hits, unchanged from v1. The three new passages are all first or second person. |
| Active voice (no "X was scanned" shapes) | Pass | Grep for `\bwas (scanned\|checked\|monitored\|built)\b`: zero hits. No passive drift in the new lines. |
| Burstiness present, every H2 section has visible sentence-length variation | Pass | Recomputed per-section standard deviation on draft-v2: intro 10.5, H2 1 9.2, H2 2 7.8, H2 3 7.7, H2 4 7.6, H2 5 6.1, H2 6 8.9, H2 7 10.1, H2 8 9.8, H2 9 11.0, CTA 11.9, FAQ 9.0. Every section is at least 1.5x the <4 uniformity threshold, most are more than double. **Direction of travel matters here:** H2 3, which received a 6-word question, went 7.0 to 7.7; H2 8, which received a 7-word question, went 9.7 to 9.8. Adding short sentences raised variation, exactly as intended. H2 1 dipped fractionally, 9.4 to 9.2, from a 29-word two-sentence beat, which is immaterial against a threshold of 4. The FAQ dropped 12.0 to 9.0 purely because the FAQ 1 trim removed the block's longest sentences; still far above threshold, and the FAQ is outside the §1.5 gate regardless. The taut prose survived intact. |
| Forbidden phrases count: `0` (list below); any hits = fail | Pass | Both lists re-greped against draft-v2, case-insensitive: `writing-standards.md` §Forbidden phrases (23 entries) and `voice.md` §Additional forbidden phrases ("None beyond the generic list"). Zero hits. |
| Em-dash count: `0` (grep `—`); any hits = automatic §1 fail, each logged in §7 as `major` | Pass | `grep -c "—"` returns 0. Matches the editor's pre-check. |
| En-dash context: every `–` hit is inside a numeric range (digits on both sides) | Pass | `grep -c "–"` returns 0, so the range condition is vacuously satisfied. |
| Intro jargon (per `writing-standards.md` §Insider-jargon translation + `audience.md` §Niche jargon translation) | Pass | Intro (lines 14-20) is untouched by the diff. No SEO/marketing jargon; none of audience.md's six insider terms appears anywhere in the body. "Analog hobbies" is still held back to H2 1 and still translated inline on first use at line 42, and the diff only changed the sentence *after* the translation, not the translation itself. |
| Product code-literal grep (modules.product on) | Pass | `product.md` defines no backticked internal code constants. Nothing to leak. Unchanged. |
| Intro-hook over-anchoring | N/A | Intro hook is a competitive-positioning observation, not a time-bound event peg. No date, launch or news hook anywhere. The post still carries no year stamp, which `brief.md` §Avoid list requires; the Oxford rewrite at line 42 deliberately did not add one. |

Forbidden phrases found (grep output from `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md` lists):
- None found.

## 2. Structure

| Check | Pass? | Notes |
|---|---|---|
| Frontmatter block present and valid per the publish adapter's frontmatter template | Pass | Byte-identical to draft-v1, which I validated in full against `adapters/publish/frontmatter/wordpress.md`. `title` quoted and 53 chars, `date: 2026-09-02`, `excerpt` 136 chars, three `tags` all exact WP term names from `blog.md` §Tag taxonomy, `authors: Olga Pak`, `draft: true`, no `cover`/`heroImage` field (correct for this adapter). No FAQPage JSON-LD requirement at the adapter level. |
| Title in frontmatter matches outline "Final title" exactly | Pass | `51 Screen-Free Hobbies to Try When Scrolling Gets Old`, character-for-character against outline line 13. Unchanged. |
| Slug / meta description match outline | Pass | Slug `screen-free-hobbies`; `excerpt` reproduces the outline's Final meta description verbatim. Unchanged. |
| H2 order in body matches outline body-sections list exactly (no additions, no removals, no reorderings) | Pass | Re-extracted both lists and compared. All nine outline H2s present, verbatim, in order (draft lines 22, 44, 63, 84, 102, 117, 130, 143, 160), plus the outline's Closing CTA block at 174. Zero deltas. The three inserted passages are all body paragraphs, not headings, so the heading structure is untouched. |
| Intro is 2-4 paragraphs, 1-3 sentences each (hook / expertise / internal-link cluster / preview) | Pass | Untouched by the diff. Four paragraphs (14, 16, 18, 20), two sentences each, 179 words. One internal link, under the ≤2 ceiling. Target keyword in the first sentence. |
| Every H2 has at least one rhythm marker (bullet list OR standalone question OR concrete number OR burstiness) | Pass | Strengthened. Seven sections carry bullet lists (58 bullets total: 51 entries + 3 tier definitions + 4 how-to bullets). H2 3 and H2 8 now additionally open on standalone question paragraphs. H2 9 carries a reader-directed question at 164, a checkable internal number ("the twelve entries in the free section") and sd 11.0. The CTA carries a named example and sd 11.9. |
| FAQ section has `### <Q>` items matching outline FAQ set 1:1 | Pass | Five `###` questions, verbatim matches to the outline's five, in the same order. The FAQ 1 edit changed only the answer body, not the question. FAQ is still the last block; nothing follows line 200. |
| JSON-LD FAQPage schema in frontmatter matches FAQ body questions 1:1 | N/A | `wordpress-rest` emits no JSON-LD at the adapter level. Sub-check correctly skipped. |
| Outro / CTA paragraph present | Pass | Untouched. Two paragraphs (176, 178), immediately before `## FAQ`, 88 words. Links `/ai-tools` with the action anchor "try my free AI tools to automate the mundane". No affiliate disclosure needed: the post carries no tagged Amazon link. |

Structural deltas vs outline (if any):
- None. Entry count re-verified independently by parsing bulleted entries with a tier parenthetical: 12 + 10 + 9 + 8 + 6 + 6 = 51, matching the title and the outline's item-count check. Every one of the 51 still carries its tier, and every tier string is byte-identical to draft-v1.

## 3. Facts + sourcing

| Check | Pass? | Notes |
|---|---|---|
| Every numeric claim traces to a `facts.md` entry OR has `[VERIFY:]` marker | Pass | Full number sweep re-run. Still zero statistics of any kind, which is correct given a deliberately empty `facts.md` §Statistics. The one sentence that reached toward a population claim in draft-v1 is gone (issue 7, see below). Remaining numbers are the same four safe classes as v1: the entry count 51 (verified against the list), the author's own biography, research-process claims traced individually, and instructional quantities carrying no factual load. |
| Research-process claims traced | Pass | All four re-checked against the sources; all four are untouched by the diff and all four still hold. Line 46 "the biggest thread I read on cheap, offline hobbies" and "came top by a distance" (r/povertyfinance, 763 score / 642 comments, top comment 659 against 277 for the next). Line 86 "a top or near-top answer in three of the five Reddit threads" (reddit.md: 3/5 exactly) and the X gardening post (x.md: @longislandviper, 993 likes, most-engaged organic post in the set). Line 132 "said flatly ... that they did not have any friends either" (verbatim in the r/povertyfinance OP). |
| Quote fidelity | Pass | All six quoted passages re-collated against `facts.md` §Quotes: lines 40, 67, 90, 154, 156, 162. All verbatim, all with correct handle and subreddit attribution, all untouched by the diff. The line 156 X quote is still truncated to "cozy, screen-free hobbies (minus the switch)", which correctly drops the source's year tail. |
| Competitor mentions framed "best for [segment]" | N/A | `modules.competitors` is off for this blog. No competitor profiles exist and none are named. |
| Pricing / feature claims about competitors match `facts.md` values | N/A | Same reason. |
| Product mentions (count: `2`, `modules.product` is on) are each earning their place | Pass | "my free AI tools" and "The Email Generator", both at line 178, both inside the closing CTA, both untouched. Deletion test: remove either and the sentence loses its subject. Under the 3-5 normal band. Still no mid-post plug anywhere in the 51 entries, and no AI tool is framed as a hobby. |
| Own-product pricing claims | Pass | Grep for `\$` returns exactly two lines, 90 and 162, both Reddit quotes with named handles. No price, tier, usage cap or feature breakdown for Olga's tools. "free AI tools" remains the durable framing. |
| Soon-to-ship feature gap check | N/A | `brief.md` §Soon-to-ship features lists "None". |
| Internal links (count: `5` blog posts, plus the `/ai-tools` CTA) placed contextually per `blog-craft.md` | Pass | Unchanged set and unchanged placement: `/how-to-reduce-screen-time` (18), `/things-to-do-instead-of-being-on-your-phone` (28), `/digital-detox-plan` (28), `/how-to-plan-your-week` (153), `/time-blocking` (153), `/ai-tools` (178). Five blog links is the top of blog-craft's 3-5 band; each sits in the section where its topic is discussed; all five were named in `brief.md` §Internal link targets. First internal link (18) still precedes first external link (42). |
| Every internal blog link is root-relative `/post-slug` per `blog.route_prefix: /`, with no trailing slash per `blog.trailing_slash: false` | Pass | All six root-relative, no trailing slash, matching `blog.route_prefix: /` and `blog.trailing_slash: false`. Grep for absolute `https://olgapak.com/...` cross-post links: zero hits. |
| External links (count: `3`) each point to reputable sources; anchor text = literal claim | Pass | Three, unchanged URLs and unchanged anchors, matching the outline's §External link plan exactly. Two of the three had their surrounding prose repaired this pass. Scope audit below. |
| No external link points to a top-10 SERP URL | Pass | Forbidden set re-extracted from `research/serp.md` §"Selected results analyzed": vogue.co.uk, theguardian.com, theworks.co.uk, theeverygirl.com, mudita.com, gamequitters.com, plus the r/simpleliving thread ranking organically. Draft hosts: corp.oup.com, health.harvard.edu, mind.org.uk. Zero intersection, and all three are additionally on blog-craft's authoritative allowlist as a health-authority / academic-publisher class. Note for the record: those three hosts *do* appear in serp.md's "Citations harvested from competitors" table, which is the correct substitution source, not the forbidden set. Reddit and X still appear only as plain-text attribution with no hyperlink. |
| Target keyword appears 2-8 times in body (actual count: `7`) | Pass | Six exact "screen-free hobbies" in body prose (line 14 intro, four H2 headings, and inside the line 156 quote) plus one singular "screen-free hobby" in the H2 1 heading, which is the editor's count of 7. Two more in the FAQ, outside the gated body. Unchanged from draft-v1; no new instance was introduced by the three insertions. Comfortably inside 2-8, no stuffing. |

**External-link scope audit** (re-run in full, because two of the three sentences changed this pass):

1. **Harvard Health, line 71. Issue 1 resolved, and the scope is now stronger than it was.** The sentence shipped as instructed, verbatim: "These hobbies borrow the thing mindfulness practice trades on: attention with somewhere to sit. [Mindfulness practice supports focus](https://www.health.harvard.edu/healthbeat/mindfulness-practice-for-focus). A row of crochet works on a similar bargain: one small repeatable motion holds your hands, so your attention stops shopping for the next notification." The citation is now a self-contained sentence whose entire subject is mindfulness practice, terminated by a full stop. The crochet effect claim is a separate sentence, and "works on a similar bargain" explicitly marks it as the author's analogy rather than a finding transferred from Harvard. This does more than move a comma: a skimming reader can no longer read the uncited clause as sitting inside the citation's blast radius, which was the exact failure I logged. The preceding analogy sentence and the anchor text are untouched, as instructed. This was the post's highest-risk claim and it is now unambiguous.
2. **Mind, line 86.** Untouched by the diff. Anchor still `spending time in nature is good for your mental health`, still scoped to nature, and the prose still says gardening "puts you outdoors on purpose" without ever claiming gardening improves your mental health. No leak. Confirmed intact.
3. **Oxford University Press, line 42. Issue 2 resolved.** The bridge shipped verbatim: "The phrase caught on because the feeling behind it is everywhere. Oxford University Press has [named "brain rot" its word of the year](https://corp.oup.com/word-of-the-year/)." The Oxford fact now anchors the cultural moment, which a word-of-the-year designation genuinely evidences, instead of being positioned as evidence for the popularity of a different phrase ("analog hobbies"). The anchor text is unchanged and no year was added, so `facts.md`'s ⚠️ flag is still honoured.

Unsourced / suspicious claims:
- None. The draft-v1 entry at line 166 ("more practice than most people manage in a month of good intentions") is gone; line 172 now reads "and four of those across a week adds up faster than a month of good intentions ever has", which is the instructed replacement verbatim. It compares the reader's own week to the reader's own intentions and makes no prevalence claim about a population. Issue 7 resolved.
- Re-run of the invented-number sweep (audience-size ranges, "N+" adoption patterns, migration/volume ranges): zero hits, unchanged.

## 4. Markers + placeholders

| Marker | Count | Placement OK? |
|---|---|---|
| `[VERIFY:]` | `0` | Yes. Still correct rather than an omission: `facts.md` §Statistics is empty by design and no claim in the draft needs one. No `\| source:` clause validation is possible or required. |
| `[EXTERNAL_LINK_NEEDED:]` | `0` | Yes. The outline's Harvard fallback path still correctly did not fire, and after the issue 1 split the sentence passes its scope test more cleanly than before. Three external links meets blog-craft's floor. |
| `[INTERNAL_LINK_NEEDED:]` | `0` | Yes. All five planned internal targets are confirmed live in `brief.md`. |
| `[IMAGE:]` | `4` vs outline `4` slots | Yes, with one placement ruling recorded below. Lines 26, 69, 88, 149, matching the outline's four in-post slots one for one. Featured-image hygiene: none uses `featured.<ext>` and none reproduces the outline's featured concept, so the cover asset will not render twice. Placement hygiene (§4.2b): no placeholder is immediately followed by a heading, so the "image sits before a heading" flag does not fire on any of the four. Table-redundancy (§4.2c): the draft contains zero markdown tables, so the `remotion` cost-tier diagram at line 26 cannot duplicate one. |

**Ruling requested by the writer: the H2 3 marker now sits after heading + question + body paragraph.** Acceptable as shipped. Leave it. Stage 4a should not nudge it. Four reasons, in order of weight:
1. The rule that exists in the skill (§4.2b) is about a marker sitting *immediately before* a heading. Line 71 after the marker is prose, so the rule does not fire. There is no violation here, only a deviation from the outline's "+1 paragraph" phrasing.
2. That phrasing exists so an image does not separate a heading from its opening prose. A one-line rhetorical question is a rhythm device, not the section's substantive paragraph. The marker still sits after the heading and after the first real body paragraph, which is what the placement plan was protecting.
3. Moving it up would be actively worse. The question at 65 ("What are your hands doing right now?") sets up the quote at 67 ("something tactile that keeps my hands busy"); they are a question-and-answer pair and an image slab between them would break the beat and orphan a one-line paragraph directly under the heading.
4. H2 8 has the identical shape (heading 143, question 145, paragraph 147, marker 149). Leaving both gives the post one internal convention. Nudging only H2 3 would give it two.

Marker-shape problems (e.g., `[Verify:]` lowercase v, extra spaces, rogue formats the Phase 4 grep will miss):
- None. Greps for `[Verify:`, `[EXTERNAL_NEEDED:`, `TODO`, `[TBD]` and `AFFILIATE-LINK-PENDING` all return zero. All four `[IMAGE:]` markers still carry the full canonical shape including `Type:` and `Suggested filename:`, and the type mix (one `remotion` diagram, three `ai-prompt` scenes) still satisfies `custom-instructions.md` §Image style's "never blanket-convert every slot to one type".

## 5. Word count

- Draft: `3,074` words (body prose: everything after the closing frontmatter `---` through the end of the CTA, excluding the `## FAQ` block, `[IMAGE:]` placeholder lines and heading text). On the editor's inclusive basis, which counts headings and placeholder lines, `3,274`.
- Outline target: `3,260` words
- Delta: `-5.7%` on the strict basis, `+0.4%` on the inclusive basis
- Band: `within ±10% = pass, no issue`. Both bases land inside the clean band, so §5 passes outright and logs nothing in §7 regardless of which basis the editor prefers. The revision added 86 words of body prose (2,988 to 3,074 strict), a 2.9% growth, which is the correct order of magnitude for three short insertions and one FAQ cut. There is no padding.

Per-section detail (strict basis, against the outline's roll-up): intro 179/200, H2 1 299/300, H2 2 405/430, H2 3 363/370, H2 4 321/340, H2 5 306/300, H2 6 231/240, H2 7 251/240, H2 8 326/400, H2 9 305/340, CTA 88/100. Every section is inside ±11% except H2 8 at -18.5%, improved from -20% in draft-v1. My iteration-1 finding on that section stands and I re-verified it: all five planned beats are present, so the section is dense rather than thin, and it does not become an issue row.

FAQ (outside the gated total): `477` words on the editor's inclusive basis, `423` counting answer prose only, down from `502` / `448`. The reduction is entirely FAQ 1. See §10 for why this is the right landing point and why it should not be cut further.

## 6. Humanization floor (per `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md`)

| Check | Pass? | Notes |
|---|---|---|
| No forbidden phrases (0 hits) | Pass | Both lists re-greped, zero hits. |
| Zero em-dashes (`—`, grep returns nothing) | Pass | Zero. |
| En-dashes (`–`) only in numeric ranges | Pass | Zero present. |
| First-person throughout | Pass | No `one`/`users`/`stakeholders` drift. |
| Active voice | Pass | No passive markers found. |
| ≥1 rhythm marker in every H2 | Pass | Bullets in seven sections; H2 9 and the CTA carry a reader-directed question, a concrete number and strong length variation; H2 3 and H2 8 now additionally open on standalone questions. |
| ≥1 bullet list per ~300 words | Pass | 58 bullets across 3,074 words of body prose, roughly one per 53 words. |
| ≥1 standalone-question paragraph per major section (if the selected author voice calls for it, per `blog-ops/profile/authors.md`) | **Pass** | Was the single failing row in review-v1; now green. Body reader-directed questions: four. Two are new standalone one-line paragraphs, at 65 ("What are your hands doing right now?") opening H2 3 and at 145 ("So which one do you actually pick?") opening H2 8, which are the two slots §9 named. Two are the pre-existing embedded openers at 106 and 164, untouched. **I am deliberately not applying the row's literal "per major section" reading to this post**, and that is a considered ruling, not leniency: I set the target myself in review-v1 §9 at exactly two standalone questions and one self-deprecating beat, and I wrote in §10 that a question opening every section would be worse than draft-v1. The floor exists to prove the voice is present, not to install a formula. Four questions across ten body sections, in a post whose voice profile names rhetorical hooks as one signature among several, is the voice being present. Combined with the line 32 self-deprecating beat, which lands voice.md §Tone's other named evidence pattern, §6 is green. |
| ≥1 concrete number or named example per major section | Pass | Unchanged and still true in every section: cribbage and gin rummy (58), blackberries and elderflower (93), sauerkraut (126), dishcloths (76), the checkable "twelve entries in the free section" (164). |
| No em-dash overuse | Pass | Zero em-dashes, so no density question arises. |
| Visible sentence-length variation in every section | Pass | Per-section standard deviation 6.1 to 11.9 across the body, all far above the <4 flag threshold, and up rather than down in both sections that received a question. |

## 7. Specific issues

Issues ranked by severity. `critical` = must fix before approve; `major` = fix if revising; `minor` = nit-pick.

**None, draft is clean.** Zero critical, zero major, zero minor.

All seven issues from review-v1 §7 are resolved and struck. For the editor's audit trail, the disposition of each:

| review-v1 issue | Line in draft-v2 | Status | Evidence |
|---|---|---|---|
| 1. Harvard citation shares a sentence with an uncited crochet claim | `71` | Resolved | Shipped verbatim as instructed. Citation is now its own terminated sentence; the crochet claim is a separate sentence explicitly marked as analogy ("works on a similar bargain"). Anchor, URL and the preceding scope sentence untouched. |
| 2. Oxford bridge cites a fact about a different phrase | `42` | Resolved | Shipped verbatim. "The phrase caught on because the feeling behind it is everywhere" is a claim the word-of-the-year designation genuinely supports. Anchor unchanged, still no year. |
| 3. Litter-picking's "no skill barrier" contradicts the learning-curve promise | `54` | Resolved | Shipped verbatim. "Visible progress, and after a few months you know one stretch of ground better than anyone else who walks it" keeps the low barrier while naming an accumulating dimension that implies a second session. The entry no longer reads as the ten-minute filler the post carves itself out from. Tier and the rest of the entry unchanged. |
| 4. Four Free-tier entries name an object without the "you already own" qualifier | `57`, `75`, `100`, `113` | Resolved | All four shipped verbatim. Sketching "in whatever notebook you already have"; hand-sewing "A needle and thread from the kit already in a drawer"; urban sketching "a pen you already own"; longhand "in a notebook you already have". No entry was retiered and no entry that already carried the qualifier was touched. Downstream benefit: FAQ 2's claim that "Free here means free in the strict sense: nothing you do not already own" is now supported by every entry it names. |
| 5. FAQ 1 recites 22 entries instead of pointing up at the list | `184` | Resolved | Cut from 89 words to 64, inside the 55-65 band specified. Six categories, exactly one example each, closing sentence retained verbatim. Now matches the outline's own direction for this question ("naming the categories and a handful of specific entries, then point up to the list rather than restating it"). FAQ 2, 3, 4 and 5 are byte-identical. |
| 6. Only two reader-directed questions, neither standalone; register runs cool | `32`, `65`, `145` | Resolved, correctly calibrated | Exactly two standalone questions in the two named slots, plus one self-deprecating two-sentence beat in H2 1. Six of ten body sections still carry no reader-directed question. Sentence-length variation rose in both question sections. See §10 ruling 3 for the full over-correction analysis. |
| 7. Unsourced prevalence claim about "most people" | `172` | Resolved | Shipped verbatim. The comparison is now the reader's week against the reader's own intentions. No population claim remains anywhere in the post. |

## 8. What the draft does well

Three specific strengths the writer should preserve on revision. Calling these out explicitly prevents the writer from rewriting strong sections accidentally. All three from review-v1 §8 survive; I re-verified each rather than assuming, and one is now measurably stronger than I described it the first time.

- **All three external links survive their scope tests, and the Harvard one is now the cleanest claim in the post.** Line 71's split turns the highest-risk sentence in the article into a self-contained mindfulness-scoped citation followed by a separately-marked authorial analogy, which is a better outcome than the original construction I called "correct". Line 86 still keeps the Mind link attached to nature and never lets it slide onto gardening. Line 42 still states the Oxford claim with no year. Nothing here should be touched again at Stage 3d or 4a.
- **Both dollar figures stay quoted and attributed, and the post never restates a price in its own voice.** Lines 90 and 162 are the only `$` occurrences in the file, both verbatim inside quotation marks with named handles. Line 162 still answers the $300 pottery breakdown with "the arithmetic is not the problem. The problem is that pottery gets sold as the cheap alternative to scrolling", refusing the price restatement `brief.md` bars while keeping the quote's force. Do not paraphrase either quote or absorb its numbers into the surrounding prose.
- **FAQ 2's free list is internally verified and correctly filtered, and it is a stronger result than I reported in review-v1.** I re-parsed it programmatically this pass. The body contains 18 unconditionally-Free entries and 7 entries with a conditional or dual tier. FAQ 2 names exactly those 18 and omits all 7 of the conditional ones. In review-v1 I credited it with omitting two; it actually omits every single one. That is the post's cost-tier credibility in miniature and it must stay exactly as it is.

Also worth not breaking, in one line each, all re-verified present and unchanged: the LEGO entry at line 80 still refuses to pretend ("It is not a cheap way to spend an evening and I am not going to pretend otherwise"), which is the tier system proving it means what it says; the three two-sided tiers at 61, 94 and 114 are still two-sided; the carve-out at 24 and 28 still defines the hobby-versus-filler distinction and still actively sends the wrong reader away to `/things-to-do-instead-of-being-on-your-phone`; the anti-hustle line at 104 is still said once, plainly, in its assigned section; the adults-only discipline holds at exactly two lines, 30 and 192, with no paediatric claim and no family framing anywhere in the 51 entries; and the CTA at 178 still bridges on time rather than features.

One new strength, from this pass: **the line 32 beat does two jobs at once.** It is the self-deprecating register the voice profile asks for, and it relocates the author's claim to authority from "I am good at these hobbies" (which would be unverifiable) to "I am stubborn about the second session" (which is the post's actual thesis). It also threads the "next session / second session" spine that runs from the intro at 16 through the carve-out at 24 to the CTA at 176. Do not cut it for length at Stage 4a.

## 9. Instructions for writer (only if verdict = `request_revisions`)

N/A. Verdict is `approve`. No further writer pass is required or wanted. The draft is done at draft-v2 and should proceed to Stage 3c/3d.

## 10. Reviewer notes (free-form)

**Rulings, in the order the editor asked for them.**

1. **Did each of the seven fixes land, and does it resolve the issue rather than gesture at it? Yes, all seven.** Eleven of the twelve replacements are verbatim reproductions of the text I specified in review-v1 §9; the twelfth (FAQ 1) is a free-composed cut that hit the specified word band and the specified structure. More important than fidelity is whether the fix does the work. Two of the seven were load-bearing and I tested both against the failure they were written for. Issue 1 was not a cosmetic comma move: the Harvard sentence now terminates before any uncited claim begins, so the only reading available to a skimmer is the correct one, and "works on a similar bargain" independently marks the crochet clause as authorial. Issue 3 was the one that touched the post's differentiator: "after a few months you know one stretch of ground better than anyone else who walks it" is a genuine accumulating dimension, not a rephrased "no skill barrier", so the entry now implies a second session like the other 50 do. Issue 4's four qualifiers additionally repair something I did not flag in review-v1: they make FAQ 2's strict-free claim true of every entry it names, so the fix propagates. Issue 5 landed at 64 words with six categories and six examples, replacing a 22-entry recital, and now matches the outline's own direction for that question. Issues 2 and 7 are exact. Nothing here gestures.

2. **Did anything regress? No, and I verified it structurally rather than by reading.** The `diff -u` is 9 hunks and 12 passages, which matches the writer's report exactly, so anything outside those 12 passages is provably unchanged. On top of that I re-ran the checks that could catch a silent regression anyway: forbidden phrases (0), em-dashes (0), en-dashes (0), third-person drift (0), passive markers (0), external hosts against the SERP forbidden set (0 intersection), `$` occurrences (2, both quoted), kid/teen/family greps (2 real hits, both the deliberate disclaimer), entry count (51), tier strings (byte-identical to draft-v1 across all 51), FAQ question set (5, verbatim), H2 order (9 + CTA, verbatim), internal links (6, all root-relative), markers (4 `[IMAGE:]`, nothing else). Every item on the editor's regression list is intact. The sentence-length variation, which was the thing most at risk from a warmth-adding pass, did not just survive: it rose in both sections that received a question and fell only in the FAQ, where it fell because a bloated answer got cut.

3. **Issue 6: correctly calibrated. This is the right amount, and it is not close to too much.** I named over-correction as the failure mode for this pass, so I owe a specific ruling rather than a shrug. What shipped is exactly the ask: two standalone question paragraphs, one in the "H2 2 or H2 3" slot and one in the H2 8 slot, plus one self-deprecating beat. The measurable case that it is not over-corrected: six of ten body sections still open on prose with no reader-directed question anywhere in them; total body questions went from 2 to 4, not from 2 to 10; the two new questions total 13 words against 86 words added overall; and per-section variation went up, 7.0 to 7.7 in H2 3 and 9.7 to 9.8 in H2 8, which is the arithmetic proof that the taut prose was not diluted to accommodate them. The qualitative case: "What are your hands doing right now?" is not a generic hook. It is physical, present-tense, and it makes the reader notice that their hands are currently holding a phone, which is the section's entire argument, and it sets up the tactile quote directly beneath it. "So which one do you actually pick?" is the sentence I dictated, and after 51 entries it is the literal question in the reader's head. The line 32 beat runs two sentences rather than one, which is marginally more than "a beat", but the second sentence carries thesis content rather than warmth, so I am not counting it against the budget. **Guidance for downstream stages: the floor is met and the budget is spent.** If the humanize-text pass or a Stage 4a copy-edit adds a third standalone question, the post crosses from "the voice is present" into "every section opens the same way", and at that point the pattern becomes the tell it was meant to avoid. Two is the number. Do not add a third.

4. **The two settled items, recorded and not re-raised.** The cannibalisation caveat I could not resolve offline is closed by the editor's live fetch: `/things-to-do-instead-of-being-on-your-phone` is organised by situation and mood, its entries are one-line prompts with no depth or cost tier, and it includes chores and to-do lists, which makes it definitionally the filler post this article carves itself out from. About 22% noun-level overlap with no shared treatment is not cannibalisation. Raw capture at `research/_raw/_cannibal_check.json` and `_cannibal_items.json`. Per my own review-v1 §10, "Walking a new route" stays: it is the most-upvoted answer in the largest thread in the research set, it is framed as exploration rather than as a way to fill ten minutes, and the two posts treat it completely differently. The `[IMAGE:]` placement question is ruled in §4 above: acceptable as shipped, no Stage 4a nudge.

**Observations that did not earn an issue row.** I looked hard for regressions at the copy level, since a verbatim-replacement pass can create local echoes. Two exist. Neither is worth an issue row and neither should trigger a writer pass.

- **Lines 56 and 57 are consecutive bullets both ending on a "notebook you already own / already have" clause.** I caused this: issue 4's fix added the qualifier to sketching, directly beneath journaling, which already had it. I looked for a better phrasing and there is not one available. "Paper you already have" is taken by origami at line 78, and dropping the qualifier reverts the fix on the post's own differentiator. In a bulleted list that gets scanned rather than read as continuous prose, the repetition reads as the tier rule being applied consistently, which is the point. Ship as-is.
- **"Actually" appears in the H2 8 heading and again in the question two lines below it** ("How to pick one, and actually stick with it" / "So which one do you actually pick?"). Also mine: I dictated that sentence. The two instances attach to different verbs and "actually" functions as a house honesty marker across this post (9 occurrences across 3,751 words of body plus FAQ, about one per 417, which is not a tic density). I would leave it. If a Stage 4a copy-editor wants it gone, "So which one do you pick?" costs the sentence some push and is the only alternative I would accept; it is optional and low-value.

**Carried forward from review-v1, still true, still not issues.** The `remotion` cost-tier diagram at line 26 still sits before the tier bullets it illustrates (36-38); the writer followed the outline's placement rule exactly and either position is defensible, so this remains an optional Stage 4a tightening rather than a defect. "Analog hobby" still appears twice (42 and 134), the second inside an attributed X paraphrase, which is fine now that the term is translated on first use. Geocaching is still the one Free entry needing a phone, deliberately, and H2 8's leakage argument still licenses it. The prose is still consistently British with no US spellings mixed in, which I would not change; it remains an operator preference question, not a defect. The FAQ at 477 words should not be cut further: the four untouched answers sit in the 83-95 word band that suits answer-engine extraction, they each do work the body cannot do for a skimmer, and the block sits outside the gated body total by design. FAQ 1 was the only one violating its own outline direction and it is now fixed.

**Confidence statement.** Everything above is verified by grep, diff, or a script run against the file, except three judgement calls I am flagging as judgement: whether the line 32 beat is the right register (I think clearly yes, against voice.md's documented evidence), whether four questions is the right density (I think yes, and I have argued it in ruling 3 rather than asserting it), and whether the H2 3 image placement is acceptable (I think yes, and moving it would be worse, per §4). I did not fetch anything; no check above depended on fetching. The cannibalisation resolution is the editor's evidence, not mine.
