# Plan Review: how to reduce screen time

Written by: plan-reviewer agent (Stage 1c.5). Read by: blog-editor (applies fixes or advances).
Independent review of `blog-ops/drafts/how-to-reduce-screen-time/plan.md`. The reviewer did NOT write the plan.

## Verdict

request_revisions

One-line rationale: the angle, SERP-gap coverage, and length call are all sound, but §Key sections 3-4 (the ten-minute settings setup and the daily-habits section) are scoped to re-cover the same specific tactics (grayscale, notifications off, home-screen decluttering, phone charging outside the bedroom, timeboxed/protected blocks) that `content/blog/how-to-stop-doomscrolling.md` and `content/blog/digital-detox-plan.md` already own in full depth on this same blog, and the plan's "resolved" overlap note only fixes a naming collision, not this content-level cannibalization risk.

## Rubric check

| Criterion | Pass? | Note |
|---|---|---|
| Angle/structure matches observed search intent (`research/serp.md` §"Search intent") | yes | Dominant intent is informational, `how-to-numbered` shape (5/7 deep-fetched results are numbered listicles). The plan matches with an actionable, ordered list of tactics, and its "friction tier" ordering directly answers `research/serp.md` §"Angle opportunities" ("No competitor orders tactics lowest-friction-to-hardest"). |
| Every planned H2 traces to a sourced fact in `facts.md` | yes | Sections 2, 3(partly via Apple/Google named examples), 5 (Iris_006 quote), 6 (redhairshanks86 laptop+phone quote), 7 (NIH/NHLBI 2-hour guidance) each trace cleanly. One soft spot: §Key sections item 1 ("Not all screen time is the problem") has no `facts.md` entry backing it; its inspiration (u/Chronically_Offline-'s "not all screentime is created equal") lives only in `research/reddit.md` §"Angle opportunities", not `facts.md`. Not a numeric claim, so it doesn't trip the blog-craft numeric-claim rule, but flag it so the writer treats the section as framing, not a cited claim. |
| Every competitor claim is fresh (`Last verified` <=14 days from today) | N/A | `blog-ops/config.yaml` sets `modules.competitors: false` for this blog; the plan makes no competitor pricing/feature claims (its one competitor-adjacent mention, third-party apps, is explicitly resolved to "describe the category, never name a tool with a price or feature claim" per `facts.md` §Rejected). Skipped per task instructions. |
| Zero forbidden SERP-competitor URLs in the external-link plan | yes | Checked all 5 rows in §External links planned (academic.oup.com, nhlbi.nih.gov, ncbi.nlm.nih.gov, support.apple.com, play.google.com) against `research/serp.md`'s top-10 set (theguardian.com, harmonyhit.com, mdanderson.org, scripps.org, healthshield.co.uk, listsof30.com, georgetown.edu). No overlap. The plan also correctly routes around the Georgetown write-up in favor of the primary PNAS Nexus study, matching `facts.md`'s linking rule. Internal links are root-relative with no trailing slash, consistent with `blog.trailing_slash: false` and `site-conventions.md` §Permalinks; both `time-blocking` and `how-to-plan-your-week` are confirmed real slugs in `content/blog/`. |
| Length target appropriate for the intent and H2 set | yes | Ruling on the explicitly flagged deviation: **accept the 2,600-3,000 word target, do not require the 3,000-5,000 `informational_pillar` band.** The math in §Length target checks out (7 deep-fetched competitors average ~1,669 words, not ~1,670 as computed independently here) and this blog's own four published sibling posts run 2,448-2,682 words (checked directly via `wc -w`, close to but not exactly the plan's claimed 2,364-3,373 range, which is a minor inaccuracy, not a wrong conclusion). Given `blog.md` §Point of view's explicit no-filler stance and the fact that this pillar is designed to link down to two sibling posts rather than re-host their full depth, padding to 3,000-5,000 words on a topic no competitor covers past ~2,600 words would manufacture filler. Accepted as-is. |
| Open questions genuinely resolved or explicitly flagged | no | The overlap question ("Overlap with `how-to-stop-doomscrolling` and `digital-detox-plan`") is marked resolved, but the resolution only prevents a *naming* collision (this post uses "effort tiers" instead of doomscrolling's "layers"). It does not address that §Key sections items 3 and 4 will, per the brief's own mandate to "cover the phone-specific mechanics ... AND the environment/habit layer," need to describe grayscale, notification muting, home-screen decluttering, charging the phone outside the bedroom, and timeboxed/protected blocks, which is the *exact same tactic set* `how-to-stop-doomscrolling.md` (its Layer 1, items 1-5, and Layer 3 items 9-10) and `digital-detox-plan.md` (Days 1, 2, 3, and 5) already cover in full depth. See §Specific issues below. |
| Keyword's dominant intent is actually covered | yes | The plan covers the "give me an ordered list of concrete actions" intent (`research/serp.md` §"Audience inferences") plus the "why" audience segment (health-consequence framing in §7), and closes the two gaps `research/serp.md` §"Angle opportunities" identifies that no competitor fills (named Screen Time/Digital Wellbeing mechanics, and what to do when limits get dismissed). |

## Specific issues (only if not `approve`)

| # | Severity | Location in plan | Issue | Required fix |
|---|---|---|---|---|
| 1 | important | §Key sections items 3-4 ("Step 2: The ten-minute setup" and "Step 3: The daily habits that hold the gains"); §Open questions row 1 | These two sections, as scoped, re-explain the same specific tactics (grayscale, notifications off, home-screen decluttering, phone-out-of-bedroom, timeboxed/protected blocks) that `how-to-stop-doomscrolling.md` and `digital-detox-plan.md` already cover at full depth on this same blog. The plan's "resolved" overlap note only fixes the "layers" vs. "effort tiers" naming collision with the doomscrolling post; it does not scope sections 3-4 to avoid re-hosting tactic-level content the siblings already own, which risks on-site content cannibalization and a repetitive read for anyone who has already read either sibling post. | Add a scoping note under §Key sections items 3-4 (or as a new bullet under the existing sequencing note) stating that these two sections cover the settings/habits at checklist depth (the action + one sentence of why), not a full tactic walkthrough, and that the pillar's unique contribution here is (a) the SERP gap of actually naming and walking through Screen Time/Digital Wellbeing app-limit setup, which neither competitor nor either sibling post does in depth, and (b) the effort-tier framing itself. Link down to `how-to-stop-doomscrolling` and `digital-detox-plan` inline at the specific tactic each covers in full (not only once, in the intro and in the limits-fail section as currently planned). Update §Open questions row 1 to state this scoping decision explicitly, since the current resolution text does not mention it. |
| 2 | minor | §Meta description (draft) | The plan claims 149 chars; the actual count is 150 chars. Still comfortably under the 160-char limit, so not blocking, but the note should be accurate. | Update the parenthetical to "(150 chars.)" or trim one character. |

## Revision instruction (verbatim, for the editor)

In `blog-ops/drafts/how-to-reduce-screen-time/plan.md`, under `## Key sections (preview, becomes the outline in Phase 2)`, add a new line directly after the existing "Notes on sequencing" line:

"Scope note for sections 3-4: cover settings and daily habits at checklist depth (the action plus one sentence of why), not a full tactic walkthrough. `how-to-stop-doomscrolling.md` already gives the full grayscale/notifications/home-screen/phone-out-of-bedroom walkthrough (its Layer 1, items 1-5, and Layer 3 items 9-10); `digital-detox-plan.md` already gives the same tactics paced across a week (Days 1, 2, 3, and 5). This pillar's unique contribution in these two sections is (a) actually naming and walking through Screen Time/Digital Wellbeing app-limit setup, the one mechanic no SERP competitor or sibling post covers in depth, and (b) the effort-tier framing itself. Link down to the relevant sibling post inline at the point each individual tactic is mentioned, not only once in the intro and once in the limits-fail section."

Then, in `## Open questions (resolved or flagged for plan review)`, edit the first bullet (currently starting "Overlap with `how-to-stop-doomscrolling` and `digital-detox-plan` — **resolved.**") to append: "This resolution also governs sections 3-4's depth: they stay at checklist level and link down per the new scope note under §Key sections, rather than re-explaining tactics the sibling posts already own in full."

Also, in `## Meta description (draft)`, change "(149 chars.)" to "(150 chars.)".
