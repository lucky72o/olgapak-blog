# Review: ai-note-taking draft-v2

Written by: `review-blog-post` skill (invoked by the editor at Stage 3b, iteration 2).
Sources: `blog-ops/drafts/ai-note-taking/draft-v2.md`, `blog-ops/drafts/ai-note-taking/draft-v1.md` (diff base), `blog-ops/drafts/ai-note-taking/review-v1.md`, `blog-ops/drafts/ai-note-taking/outline.md`, `blog-ops/drafts/ai-note-taking/facts.md`, `blog-ops/drafts/ai-note-taking/brief.md`, `blog-ops/drafts/ai-note-taking/research/serp.md`, `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md`, `${CLAUDE_PLUGIN_ROOT}/standards/blog-craft.md`, `adapters/publish/frontmatter/wordpress.md`, `blog-ops/profile/voice.md`, `blog-ops/profile/authors.md`, `blog-ops/profile/audience.md`, `blog-ops/profile/product.md`, `blog-ops/profile/blog.md`, `blog-ops/profile/custom-instructions.md`, `content/blog/digital-vs-paper-notes.md`.
Read by: editor (decides next action), blog-writer (consumed as `review_path` when `mode=revise`).

**Purpose:** a deliberately critical, checklist-driven audit of the draft against every objective rule in the outline + brand docs. The review skill is independent of the writer so it isn't biased by the same prompts that wrote the draft.

## Reviewed

- Draft: `blog-ops/drafts/ai-note-taking/draft-v2.md` (word count: 2,732 body prose excluding FAQ and `[IMAGE:]` lines; outline target: 2,530)
- Outline: `blog-ops/drafts/ai-note-taking/outline.md` (status: `approved`)
- Author voice: `olga` (from `brief.md`)

## Verdict

`approve`

Reasoning (1–3 sentences): All six numbered items from `review-v1.md` §9 were applied exactly as written and nothing else was disturbed, and the cannibalisation bar §10 set for issue 1 is cleanly cleared: the expanded H2 4 adds zero new tool names, zero numbered product lists, zero rankings and zero superlatives, closing the shopper gap with five category-agnostic criteria and one lecture-row paragraph instead. Every hard gate stays green (0 forbidden phrases, 0 em-dashes, 0 forbidden-SERP external links, 0 prices, 0 unsourced numbers, correct marker shapes and counts, word count +8.0% inside ±10%), and the two `minor` issues logged below are copyedit-grade, do not block approval, and are cheaper for the editor to apply directly than to route through a third writer pass.

**Iteration counter:** this is revision pass `2` of `max 2`. If verdict = `request_revisions` AND iteration > 2: the editor should escalate to the human, not dispatch another writer pass. Not triggered, verdict is `approve`.

---

## 0. Revision compliance audit vs `review-v1.md` §9 (iteration-2 gate)

Full `diff` of draft-v1 against draft-v2 run first. **Fourteen changed hunks, all fourteen traceable to a §9 item or to the two changes the writer self-declared. Zero unexplained edits, zero collateral damage to unflagged sections.**

| §9 item | Required | Applied? | Evidence |
|---|---|---|---|
| **1. H2 4 depth without a roundup** | (a) 4–5 category-agnostic pre-commit criteria after the table; (b) one prose paragraph on the lectures/long-recording case; hard constraints: no tool name beyond NotebookLM, no ranking, no scoring, no "best overall", no numbered product list, keep the line-121 refusal verbatim | ✅ **Fully, and within the constraints** | (a) Lines 135–141: a five-item bolded criteria list, every item category-level, drawn from what the post already establishes (room-vs-call capture, chaptering/timestamps, transcript export, source-passage traceability, where the audio goes). (b) Line 143: the lecture-row paragraph, anchored on the chaptered-stats-lecture illustration from H3 3.2 exactly as instructed ("chaptered by topic, timestamps sitting on the worked examples, the way step two described"). Constraint compliance audited separately below. Line 121's "I am not going to rank these" survives byte-identical. |
| **2. Six British spellings → US** | `judgement`→`judgment` (29), `travelled`→`traveled` (45), `favouring`→`favoring` (47), `behaviour`→`behavior` (49), `recognise`→`recognize` (95), `favouring`→`favoring` (182) | ✅ All six | Now at draft-v2 lines 29, 47, 49, 51, 97, 196. A full British-spelling sweep (`judgement\|travelled\|favour\|behaviour\|recognise\|organise\|summaris\|centre\|colour\|programme\|analyse\|realise\|practise\|licence\|defence\|labour\|neighbour\|catalogue\|apologise\|prioritis\|utilis`) returns **zero remaining hits**. |
| **3. Invented personal-discovery clause** | Replace "where I first saw this workflow described" with "about this workflow", keep the rest | ✅ Exact | Line 77 now reads "on the r/LifeProTips thread about this workflow, the single most-upvoted reply argued…". The most-upvoted-reply attribution is intact. No new anecdote introduced anywhere in the diff. |
| **4. Replication attribution split** | Verbatim rewrite restoring the 2019 date and separating the re-run finding from the meta-analysis finding; keep link, anchor, position | ✅ Verbatim, character-for-character | Line 49. Matches the §9 block exactly. See §3's retention audit for the consistency re-check against the published post. One copyedit artifact this rewrite introduced is logged as issue 1 in §7. |
| **5. Two standalone-question paragraphs** | One in H2 1 (before the extractive/abstractive explanation), one in H2 4 (after the table, before the new criteria list). "Two only, do not add one to every section." | ✅ Exactly two, both in position | H2 1 line 35: "So why does an AI summary sometimes contain a sentence nobody said?" placed immediately before the gloss at line 37. H2 4 line 133: "So how do you choose inside a row?" placed after the table (125–131) and before the criteria list (135). No third question was added anywhere. Body sections with a standalone-question paragraph: 3 of 6, up from 1. |
| **6. Attribute the NotebookLM free claim** | "because it is free" → "because Google offers it free"; leave link, anchor and the rest of the paragraph untouched | ✅ Exact | Line 145. The link, the anchor text "answers using only the sources you upload", the "That second half matters more than it sounds" beat and the "It is not a live meeting recorder, though" limitation are all unchanged. |

**Marker preservation:** all 4 `[IMAGE:]` and both `[EXTERNAL_LINK_NEEDED:]` markers survive the revision unchanged, in the same positions relative to their sections. No marker was removed, reworded, or resolved. The §9 block's lock list (H2 order, title, slug, meta description, FAQ set) is fully respected.

### 0.1 Cannibalisation audit (the §10 bar for issue 1) — **PASS, checked hard**

`review-v1.md` §10 set an explicit escalation: a second named third-party tool, a numbered product list, a ranking, or any superlative in the revised H2 4 is a `critical` finding at iteration 2. Every clause was tested independently against the whole draft, not just the diff.

| Test | Method | Result |
|---|---|---|
| A second named third-party tool | Grepped the full draft case-insensitively for 30+ candidate product names likely to be reached for in this category (`otter`, `notta`, `plaud`, `notegpt`, `zoom`, `metaview`, `granola`, `fireflies`, `tl;dv`/`tldv`, `obsidian`, `notion`, `evernote`, `onenote`, `goodnotes`, `mem.ai`, `rev.com`, `descript`, `supernormal`, `fathom`, `read.ai`, `gemini`, `copilot`, `chatgpt`, `claude`, `whisper`, `apple notes`, `voicenotes`, `superhuman`) | **Zero hits.** The draft names exactly one third-party product, NotebookLM, at lines 145 and 200, both pre-existing from draft-v1. The three other proper nouns in the body are Microsoft (line 33, named as the *author of a guide*, not a tool being assessed), Google (line 145, named as NotebookLM's publisher per §9 item 6) and Cornell (lines 61, 103, a note-taking method and a university, not a product). OpenAI (line 17) is Olga's own build stack in the expertise statement. |
| A numbered product list | Grepped `^[0-9]+\. ` across the whole draft | **Zero hits.** No ordered list of any kind exists in the post. The new list at 137–141 is an unordered list of *questions to ask*, not of products. |
| A ranking | Grepped `\brank\b\|\branked\b\|\branking\b\|\bscore\|\bwinner\b\|\b#1\b\|\bnumber one\b\|\btop \d` | Hits at line 121 ("I am not going to rank these, because the ranking depends entirely on what you are recording") and line 200 ("Rather than hunting for one winner"), both of which are the post **refusing** to rank. No ranking is performed. |
| A superlative | Grepped `\bbest\b\|\bmost accurate\b\|\bthe most \|strongest` | Three hits, all pre-existing and all cleared: line 37 "the sentences that already carry the most meaning" (the extractive-summarization gloss, not a product claim); line 198, the FAQ question "What is the best free AI note-taking tool?", which is verbatim from the approved outline's FAQ set and locked; line 200, "NotebookLM is the strongest free option I know", unchanged since draft-v1, first-person-hedged, scoped to one tool rather than a ranked field, and immediately followed by the outline-mandated deflection "Rather than hunting for one winner, match the tool to the job". **`facts.md` §Rejected's banned "most accurate" claim appears nowhere.** See §10 for why this FAQ line is not being escalated. |
| Net effect on the section's posture | Read 119–147 end to end | The expansion makes the refusal *more* credible, not less: a reader who arrives shopping now leaves with five transferable evaluation questions and a good-vs-bad description of the lecture case, none of which names a product. This is the outcome review-v1 asked for. |

**No `critical` cannibalisation finding.** The `ai-study-tools` roundup queued at `content-plan.md` row 18 remains uncannibalised.

### 0.2 The writer's two self-declared out-of-scope changes — both cleared

**(1) H2 1 reword, line 39.** draft-v1's "That second kind is why an AI summary sometimes contains a line nobody in the room ever said" → draft-v2's "That second kind is the culprit."

Justified and correctly executed. Issue 5's suggested question text ("So why does an AI summary sometimes contain a sentence nobody said?") is a near-duplicate of the v1 clause it now sits two paragraphs above; leaving both would have posed the question and then restated it as an answer in the same breath. The writer moved the semantic content up into the question rather than deleting it.

**The `audience.md` §Reading posture gloss is intact and still lands before first use, verified directly.** The terms `extractive` and `abstractive` appear at exactly two places in the draft: line 37 and line 155. Line 37 is the first occurrence, and it *is* the gloss ("Extractive summarizing picks out the sentences that already carry the most meaning and shows you those. Abstractive summarizing writes new sentences that say the same thing in fewer words."). Line 155's "that abstractive rewriting from earlier" is 118 lines downstream and explicitly back-references it. The new question at line 35 uses no jargon at all. So the sequence is now **need (35) → gloss (37) → payoff (39)**, which is a stronger teaching order than v1's **gloss (35) → payoff (37)**, because the question earns the definition before it arrives. The outline's required content ("that second kind is why an AI summary sometimes contains a sentence nobody in the room ever said") is fully preserved, redistributed across the question and its answer rather than carried by one sentence. Antecedent check: "the culprit" at line 39 resolves unambiguously to line 35's question, which line 37 opens by explicitly answering ("The answer is one piece of jargon worth translating"). No dangling referent. **No regression, and a small improvement.**

**(2) `study centre` → `study center`, line 61.** Correct, and it was a genuine gap in §9 item 2's six-item list. "study center" is a descriptive noun phrase, not the proper name of the entity (which is Cornell's Learning Strategies Center at `lsc.cornell.edu`), so US spelling is right rather than an incorrect Americanization of a proper noun. It is also now the only `cent(er|re)` token in the draft, so there is no internal inconsistency. Applying it was the right call; leaving it would have been the seventh British spelling in a file whose other six were being normalized in the same pass.

---

## 1. Voice + tone

| Check | Pass? | Notes |
|---|---|---|
| Matches `author_voice`: `olga` (§`blog-ops/profile/voice.md` + `blog-ops/profile/authors.md`) | ✅ | Warm, first-person, lightly self-deprecating, unchanged in register from draft-v1. "Ouch." (line 157) still lands `voice.md` §Lexicon's one-word rhythm beats; "This is the one that got me" (157) is in register. `authors.md` olga entry's "mixes punchy short sentences with longer explanatory ones" is measurable, see burstiness row. The v1 nit (only one rhetorical hook) is now materially improved: 3 of 6 body sections carry one, up from 1. The two new questions (35, 133) are both written in the olga register, short and reader-directed, not stiff transitional filler. |
| First person throughout (`I`, `we`, `you`), no `one`, `users`, `stakeholders` | ✅ | Grep for `\b(one could\|one should\|users are\|stakeholders)\b` returns zero hits. "One student" (83), "One person on r/LifeProTips" (117) and "Someone on r/AI_Agents" (147) are counted persons, not the indefinite pronoun. The new H2 4 content is written in second person to the reader ("before you commit", "whichever row you are in", "you are back to dragging a slider around"), consistent with the rest. |
| Active voice (no "X was scanned" shapes) | ✅ | Grep for `\bwas (scanned\|checked\|monitored\|built)\b` returns zero hits. The passives that do appear ("is often ruled out by workplace policy", 127; "everything needs reviewing", 131) are agentless by design and read naturally. No new passive constructions in the added content. |
| Burstiness present, every H2 section has visible sentence-length variation | ✅ | Recomputed per-section sentence-length stddev on draft-v2, all far above the <4-word fail threshold: H2 1 sd=7.5 (min 4 / max 32), H2 2 sd=16.3 (6/67), H2 3 sd=13.0 (4/65), H2 4 sd=11.3 (8/53), H2 5 sd=9.2 (4/32), H2 6 sd=12.1 (4/48), CTA sd=11.0 (9/38), FAQ sd=8.1 (7/33). H2 4's stddev moved 14.0 → 11.3 with the new content, still 2.8× the threshold; the criteria bullets are deliberately parallel in shape, which is correct for a checklist and does not read as uniform prose. |
| Forbidden phrases count: `0` (list below); any hits = fail | ✅ | All 23 base phrases from `writing-standards.md` §Forbidden phrases grepped case-insensitively against draft-v2. `voice.md` §Additional forbidden phrases declares "None beyond the generic list", so there is no profile extension to check. |
| Em-dash count: `0` (grep `—`); any hits = automatic §1 fail, each logged in §7 as `major` | ✅ | `grep -o '—' \| wc -l` returns 0. |
| En-dash context: every `–` hit is inside a numeric range (digits on both sides) | ✅ | `grep -o '–' \| wc -l` returns 0, so the check is vacuously satisfied. Note the new content at 135 writes "Five things" as a word rather than reaching for a numeric range, so no en-dash risk was introduced. |

Forbidden phrases found (grep output from `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md` lists):
- None found.

Additional §1 sub-checks:

- **§1.7 Intro jargon check.** Intro spans lines 15–21 (4 paragraphs, between the closing frontmatter `---` and the first `## `), **byte-identical to draft-v1**. Re-grepped anyway: the `writing-standards.md` SEO/marketing baseline (`share a SERP`, `same SERP`, `funnel`, `top of funnel`, `bottom of funnel`, `featured snippet`, `rich snippet`, `E-E-A-T`) plus this blog's own list from `audience.md` §Niche jargon translation (`SERP`, `DR`, `Domain Rating`, `keyword difficulty`, `Parkinson's Law`, `timeboxing`, `Kanban`) plus the AI-specific terms `brief.md` §Avoid list names (`LLM`, `ASR`, `RAG`, `embeddings`, `token`). **Zero hits.** The post's one piece of specialist vocabulary is still held back to H2 1 and glossed there. **Pass.**
- **§1.8 Product code-literal grep.** `modules.product` is on. `blog-ops/profile/product.md` defines no backticked code constants, status enums, or table/component names, so there is nothing to grep for. **N/A.**
- **§1.9 Intro-hook over-anchoring.** The intro's hook (line 15) is a position ("the pitch is that you never have to take notes again"), not a time-bound event, price hike, or news peg. Check skipped per the skill's own escape clause. **N/A.** Worth noting the revision did not introduce one: the added H2 4 content contains no launch, no date, and no "recently".

## 2. Structure

| Check | Pass? | Notes |
|---|---|---|
| Frontmatter block present and valid per the publish adapter's frontmatter template (title, date, excerpt, tags, authors, cover, JSON-LD) | ✅ | **Unchanged from draft-v1**, re-verified against `adapters/publish/frontmatter/wordpress.md`. All required fields present and filled: `title` (quoted, 54 chars, inside the template's 50–60 band), `date: 2026-09-09`, `excerpt` (148 chars, ≤160), `tags` (4 entries), `authors: [Olga Pak]` matching the `olga` entry's byline in `authors.md`, `draft: true`. Correctly carries **no** `cover`/`heroImage` field per that template's §No cover/heroImage field. All 4 tags (`ai`, `ai tools`, `note-taking`, `students`) re-checked against `blog.md` §Tag taxonomy and all 4 are real live WP terms. |
| Title in frontmatter matches outline "Final title" exactly | ✅ | `AI Note-Taking: A 5-Step Workflow That Actually Sticks`, byte-identical to outline §Final title. |
| Slug / meta description match outline | ✅ | Slug carried by the draft directory (`ai-note-taking`); the WP frontmatter template defines no slug field. `excerpt` is byte-identical to outline §Final meta description. |
| H2 order in body matches outline body-sections list exactly (no additions, no removals, no reorderings) | ✅ | Draft-v2 H2s in order (lines 23, 41, 63, 119, 149, 167, 182, 188): "What AI note-taking actually does (and what it doesn't)" / "Why you still take the notes, and AI does the rest" / "My 5-step AI note-taking workflow" / "Which AI note-taking tool fits which job" / "Where AI note-taking still falls down" / "Before you hit record: permission, policy and privacy" / "Start with the mundane half" (the outline's §Closing CTA) / "FAQ". Exact 1:1 match with outline §Body sections H2 1–6 plus the planned CTA and FAQ. Identical to draft-v1's order; the revision added no heading and moved none. |
| Intro is 2–4 paragraphs, 1–3 sentences each (hook / expertise / internal-link cluster / preview) | ✅ | 4 paragraphs, 1–2 sentences each, unchanged. P1 hook carries the target keyword. P2 is the expertise statement drawn from `voice.md` §Credibility message. P3 carries exactly 1 internal link (cap is 2). P4 is the preview. |
| Every H2 has at least one rhythm marker (bullet list OR standalone question OR concrete number OR burstiness) | ✅ | All eight clear the bar, and the margin improved: **every body H2 now carries a bullet list**, which was not true in draft-v1 (H2 4 had none). See §6 for the stricter humanization-floor breakdown. |
| FAQ section has `### <Q>` items matching outline FAQ set 1:1 | ✅ | 4 `###` items under `## FAQ` (lines 190, 194, 198, 202), matching all 4 outline FAQ questions verbatim, same order. Locked set respected; the revision touched only the US-spelling fix inside the second answer (line 196). |
| JSON-LD FAQPage schema in frontmatter matches FAQ body questions 1:1 | N/A | `publish.adapter: wordpress-rest`. Per the skill's §2.1 and the WP frontmatter template's §FAQ (schema markup), FAQPage JSON-LD is a WP-plugin concern and this adapter emits none. Sub-check skipped by rule, not by omission. |
| Outro / CTA paragraph present | ✅ | H2 "Start with the mundane half" (182–186), two short paragraphs, sits immediately before `## FAQ` with **nothing after the final FAQ answer** (line 204 is the last line of the file). Action anchor `Try my free AI tools` → `https://olgapak.com/ai-tools`, matching `blog.md` §Primary CTA target and hook exactly. No feature re-list, no "in conclusion". |

Structural deltas vs outline (if any):

- **One authorized delta, documented rather than failed.** H3 3.4's heading now reads "Step 4, Organize: give the output a shape you'll **recognize** later" (draft line 97) against `outline.md` line 155's "…you'll **recognise** later". This is `review-v1.md` §9 item 2, which explicitly ordered the change and explicitly characterized it as "a spelling normalization only, not a structural change" on the grounds that "Organize" and "recognise" cannot both be right in the same heading. The skill's §2.3 diff is scoped to `## ` headings, and the H2 order is byte-identical, so this does not fire the §2 structural-delta branch of the verdict tree. **Editor action (documentation only, not a draft defect):** backfill `outline.md` line 155 to `recognize` so the two artifacts stop disagreeing in the archive.
- No other drift. Title, meta, H2 order, the other four H3 headings, FAQ set, intro shape, CTA placement and internal-link distribution all match `outline.md` as approved.
- Absolute cross-post link check: grepped for `https?://(www\.)?olgapak\.com/<blog-slug>` shapes. The only two `olgapak.com` URLs in the draft (95, 186) both point at `/ai-tools`, the marketing/tools page, not a blog post. `blog-craft.md` §Internal linking routes app/marketing-site links to the absolute form. **No issue.**

## 3. Facts + sourcing

| Check | Pass? | Notes |
|---|---|---|
| Every numeric claim traces to a `facts.md` entry OR has `[VERIFY:]` marker | ✅ | Re-audited in full, including the new content. "45 minutes to 10" (117) → `facts.md` §Statistics row 3, still correctly hedged as "one person's experience rather than a general figure". "more than thirty of these tools" / "three months" (83, 87) → `facts.md` §Quotes, the 30+-tools student, attributed without a link. "the single most-upvoted reply" (77) → `facts.md` §Statistics row 4 (the draft states the rank without printing the 970 vote count). "four months" (147) → `facts.md` §Quotes, u/Affectionate-Good168. "three or four businesses" (180) → `facts.md` §Named examples, the multi-vendor pipeline, framed with "commonly"/"frequently"/"can" per that row's `needs_verification` instruction. "at least a dozen" (165) is inside a verbatim quote. **New in this pass:** "in 2019" (49) → `facts.md` §Statistics row 2, which dates the re-run to 2019, and matches the published `digital-vs-paper-notes` post; "Five things" (135) is a self-referential count of the list immediately below it, verified as 5 bullets; "a 75-minute recording" (143) is a stipulated illustrative session length explicitly specified by `review-v1.md` §9 item 1(b), not a measured claim about anything. **Zero unsourced numeric claims.** |
| Every competitor name used is discussed with a "best for [segment]" framing, not a takedown | N/A | `modules.competitors` is **off** for this blog (`config.yaml`), so there is no `facts.md` §Competitor facts table and no profile-backed competitor set to check. See the substitute audit below. |
| Pricing / feature claims about competitors match `facts.md` values (verify dates not stale) | N/A | Same reason; no `Last verified` freshness check applies. Separately: **zero prices appear anywhere in the draft.** Grep for `\$[0-9]`, `per month`, `/month`, `per seat`, `free tier`, `free up to`, `subscription`, `pricing` and `credits` returns nothing. The rejected tl;dv price from `facts.md` §Rejected did not leak in, and the revision did not introduce a price while adding shopping criteria, which was the obvious failure mode for a "what to check before you commit" list. The list deliberately asks about capability and data flow, never cost. |
| Product mentions (count: `2`, if `modules.product` is on) are each earning their place, none can be deleted without losing meaning | ✅ | Exactly the two the outline authorized, both unchanged by the revision: one in the body at H3 3.3 (line 95, the Text Summarizer, gated behind a real precondition, "When the source is already text rather than audio"), and one in the closing CTA (line 186). Deletion test: line 95's sentence loses its whole point without the tool, it exists to answer "what do I do when there is no audio to transcribe". No extras, no stuffing; grep for `olgapak\|text summarizer\|text simplifier` returns hits at 95 and 186 only. No usage, adoption, or performance figure for Olga's tools appears anywhere, per `product.md` §First-party data availability (`hypothetical`) and `brief.md`. "free" is used as a durable framing, which `blog-craft.md` §Own-product pricing claims explicitly permits. |
| Internal links (count: `5`) exist in the intro cluster per `${CLAUDE_PLUGIN_ROOT}/standards/blog-craft.md` | ✅ | 5 total, inside the 3–5 band, distribution unchanged from draft-v1 and matching the outline's plan exactly: `/digital-vs-paper-notes` (19, intro), `/cornell-note-taking-method` (61, H2 2), `/how-to-take-notes-on-ipad` (75, H3 3.1), `/note-taking-methods` (103, H3 3.4, the silo-UP link to the pillar), `/how-to-plan-your-week` (117, H3 3.5). Only 1 in the intro against a cap of 2, no "see also" stack. The first internal link (19) precedes the first external link (47). The revision added no internal link, which is correct: H2 4's expansion had no contextually relevant published target. |
| Every internal blog link is root-relative `{route_prefix}<slug>` (trailing slash iff `blog.trailing_slash: true`), not `https://yourblog.com{route_prefix}...` (absolute = `major`) | ✅ | All 5 are root-relative with **no** trailing slash, matching `blog.route_prefix: /` and `blog.trailing_slash: false` in `config.yaml`. |
| External links (count: `4` rendered + `2` markers) each point to reputable sources; anchor text = literal claim | ✅ | Unchanged set from draft-v1. See the per-link forbidden-SERP audit below. Anchor texts are literal claims in all four cases. |
| Target keyword appears 2–8 times in body (actual count: `7`) | ✅ | Exact-phrase "AI note-taking" in the body prose at lines 15, 21, 23, 63, 119, 149, 165. Comfortably inside the 2–8 band, unchanged by the revision (the new H2 4 content adds no keyword repetition, which is the right instinct). Frontmatter (2) and FAQ (1) occurrences excluded from the count per the skill's body-only basis. |

**§3.5 Forbidden external links to SERP competitors, per-link verdict.** This check is never module-gated and was re-run in full on draft-v2 rather than inherited.

Forbidden set, from `research/serp.md` §Selected results analyzed (`zoom.com`, `metaview.ai`, `uk.pcmag.com`, `medium.com`, `microsoft.com`) plus the nine top-10 domains named in `facts.md` §Link-safety note: `zoom.com`, `metaview.ai`, `pcmag.com`, `medium.com`, `microsoft.com`, `plaud.ai`, `notta.ai`, `notegpt.io`, `otter.ai`. Allowlist: `blog-craft.md` §Authoritative-site allowlist; `voice.md` §Additional allowlist domains declares "None beyond the generic allowlist", so no profile extension.

| # | Link target | Anchor | Verdict |
|---|---|---|---|
| 1 | `https://journals.sagepub.com/doi/full/10.1177/0956797614524581` (line 47) | "laptop note-takers recorded more of the lecture word for word and did worse on conceptual questions" | ✅ Not in forbidden set. Academic publisher, primary source, matches `facts.md` §Statistics row 1 and outline §External link plan row 1. Anchor = literal claim. |
| 2 | `https://link.springer.com/article/10.1007/s10648-019-09468-2` (line 49) | "performance did not consistently differ between any of the groups" | ✅ Not in forbidden set. Academic publisher, primary source, matches `facts.md` §Statistics row 2 and outline plan row 2. Anchor = literal claim, and the §9 item 4 rewrite left both URL and anchor text untouched. |
| 3 | `https://lsc.cornell.edu/notes.html` (line 61) | "a cue-and-recite loop" | ✅ Not in forbidden set. `.edu`, allowlist-class by rule. Matches outline plan row 3. The `centre`→`center` edit on this line touched prose only, not the link. |
| 4 | `https://support.google.com/notebooklm` (line 145) | "answers using only the sources you upload" | ✅ Not in forbidden set. `support.google.com` is named explicitly in the allowlist's §Official platform docs. Matches outline plan row 4. Its `facts.md` row is `needs_verification`; Stage 3d confirming the wording (and now also the free tier, per §9 item 6) before this link ships is the outline's own plan, not a review defect. |

Full-draft grep for all nine forbidden domains, in **any** position (link target, bare URL, or bare reference), returns **zero hits**. The two facts sourced to microsoft.com (the five-step model at 33 and the extractive/abstractive translation at 37) and the fact sourced to medium.com (the chaptered stats lecture at 83, re-used in the new lecture paragraph at 143 as "the way step two described") are attributed in prose without links, exactly as the outline instructed. **Note that the new line 143 re-uses a medium.com-sourced illustration and correctly does so by back-referencing the post's own step two rather than re-attributing or linking, so the expansion introduced no new sourcing surface at all.** Reddit and X provenance is named in prose only (`r/askanything`, `r/LifeProTips`, `r/AI_Agents`, `@wojakcodes on X`) with **no** reddit.com or x.com URLs rendered anywhere; grep for `reddit.com` and `x.com` returns zero. **Zero critical link findings.**

**§3.6 / §3.7 Competitor freshness and `[VERIFY:]` on competitor pricing/features:** N/A, `modules.competitors` is off. Substitute audit, per the outline's carried constraint that every capability claim about a named third-party tool must be attributed opinion or carry a sourced `[VERIFY:]`: the draft names exactly **one** third-party tool, NotebookLM (145, 200), and its two capability claims (source-only answering, click-through citations) both trace to `facts.md` §Named examples and are anchored to Google's own help docs, with the free claim now attributed to Google rather than asserted flatly. **Every statement in the new criteria list (137–141) and the new lecture paragraph (143) is written at the category or capability-question level** ("Does it hear the room, or only the call?", "A good tool turns a 75-minute recording into something you can navigate"), never as a claim about a named product, which is exactly how to satisfy this constraint with `modules.competitors` off. H2 4's table (125–131) remains category-level throughout. Microsoft is named at 33 as the author of a guide, not as a tool being assessed. No "most accurate" claim appears anywhere.

**§3.8 Invented-number check.** Grepped for audience-size ranges (`\d+K to \d+K`, `\d+,?\d{3} to \d+,?\d{3}`, `\d+K-\d+K`), "N+" adoption claims, and migration/volume ranges (`moving \d+ to \d+`, `cataloging \d+\+`). **Zero hits**, including across the new content. No fake precision was introduced by the expansion, which is the failure mode a "what to check" list invites (e.g. "look for 95%+ accuracy"). The list asks binary capability questions instead, with no thresholds.

**§3.9 Own-product pricing claims.** Zero `\$\d` hits and zero usage-cap phrasings anywhere in the draft. **Pass.**

**§3.10 Soon-to-ship feature gap check.** `brief.md` §Soon-to-ship features records "(None known.)", so there is no feature to check for gap framing. **N/A.**

**Retention-claim consistency audit vs `content/blog/digital-vs-paper-notes.md`** — re-run in full on draft-v2, because §9 item 4 rewrote the load-bearing sentence.

- The draft never claims handwriting beats typing, and never claims handwriting improves memory. Grepped both shapes across the whole file including the FAQ.
- The mandated sequence is intact and now tighter: **original finding (47) → 2019 replication (49) → mechanism claim (51)**, in three consecutive paragraphs.
- Line 47 cites the original finding with the same anchor text and the same sagepub URL the published post uses at its line 46. Consistent.
- Line 49's rewrite is now **closer** to the published post than draft-v1 was. The published post reads "When other researchers ran the study again in 2019, [some trends suggested longhand superiority, but performance did not consistently differ between any of the groups], including a group that took no notes at all. Their meta-analysis of the direct replications found small effects favoring longhand that weren't statistically significant." Draft-v2 reads "Then researchers re-ran it in 2019. [performance did not consistently differ between any of the groups], including a group that took no notes at all, and a meta-analysis of the direct replications found the small effects favoring longhand were not statistically significant." Same two findings, same order, same date, same URL, same anchor. The v1 conflation of the re-run with the meta-analysis is gone. **The `critical` risk this axis carries is fully retired.**
- Line 47 still names the flattening out loud ("That result traveled fast and got flattened into 'write it by hand'"), mirroring the published post's "it got flattened into 'handwriting improves memory'".
- Line 51 states the only claim this blog is allowed to make ("Which tells you the pen was never the active ingredient… That choosing and rephrasing is where the understanding happens"), matching the published post's "The mechanism it points to is what you do while you take notes, not the keyboard itself."
- The FAQ answer at 196 repeats the honest version rather than reverting to the folk version, and its `favouring`→`favoring` edit changed spelling only.
- **No contradiction with the published correction. Zero critical findings on this axis.** One copyedit artifact of the rewrite is logged as issue 1 in §7.

Unsourced / suspicious claims:
- None. Every numeric and factual claim in the body traces to a `facts.md` row, sits inside an attributed quote, or is carried by one of the two `[EXTERNAL_LINK_NEEDED:]` markers.

## 4. Markers + placeholders

| Marker | Count | Placement OK? |
|---|---|---|
| `[VERIFY:]` | `0` | ✅ N/A. Zero markers means §4.4's `\| source:` clause validation has nothing to validate, and it means the writer again found a `facts.md` row or an attributed quote for every claim rather than deferring work to Phase 4. Notably the H2 4 expansion added **zero** new `[VERIFY:]` markers, because it added zero new factual claims. |
| `[EXTERNAL_LINK_NEEDED:]` | `2` | ✅ Expected count, exactly the two the outline planned, both preserved verbatim through the revision. Line 153 (speech-recognition accuracy varies with accent/crosstalk/audio conditions, in H2 5) and line 174 (recording-consent rules differ by jurisdiction, in H2 6). Both carry the required `<claim + suggested source type>` payload. Both remain placed where the outline earmarked them, and both surrounding passages **still read correctly if Stage 3d finds nothing**: 153 immediately follows with "The practical answer doesn't need a study behind it", and 174 follows with "This is not something to guess at, and it isn't something I can answer for you". |
| `[INTERNAL_LINK_NEEDED:]` | `0` | ✅ Expected. All five internal links resolved to real published slugs. |
| `[IMAGE:]` | `4` vs outline `4` in-post slots | ✅ Exact match, unchanged from draft-v1. |

**§4.2 Image slot diff.** Outline §Image placement plan specifies 1 featured (frontmatter cover, `ai-prompt`) + 4 in-post. The draft carries exactly 4 in-post `[IMAGE:]` placeholders, 1:1 against the outline's four in-post slots, same order, same types. Line numbers shifted by the revision; concepts and types did not.

| Draft-v2 line | After | Concept | Type | Matches outline slot |
|---|---|---|---|---|
| 45 | H2 2 + 1 para | Two-column "AI handles / you handle" | `remotion` | ✅ slot 1 |
| 67 | H2 3 + 1 para | Linear five-step process diagram | `remotion` | ✅ slot 2 |
| 123 | H2 4 + 1 para | Same person in three capture situations | `ai-prompt` | ✅ slot 3 |
| 171 | H2 6 + 1 para | Phone face-up between two people, asking before recording | `ai-prompt` | ✅ slot 4 |

**§4.2a Featured-image hygiene.** ✅ **Pass, and worth restating for this pass.** No `[IMAGE:]` placeholder carries `Suggested filename: featured.<ext>` (the four are `ai-handles-you-handle.png`, `five-step-workflow.png`, `capture-situations.png`, `ask-before-recording.png`), and none of the four concepts matches the outline's featured concept ("the split between the mundane half of note-taking that AI handles and the thinking half that stays yours"). The nearest neighbour is slot 1, but the featured slot is `ai-prompt` per `images.featured_default` and `custom-instructions.md` §Image style while slot 1 is `remotion`: different renderings for different purposes. The featured image ships via media upload with no inline embed and no frontmatter cover field, so the post will not render the same asset twice.

**§4.2b Image-placement hygiene.** ✅ Pass on all four. No placeholder is immediately followed by an `^## ` or `^### ` heading (45→prose at 47, 67→prose at 69, 123→the table at 125, 171→bullets at 173). Each sits after its H2 and after that section's first body paragraph, exactly as the outline planned. Slot 3 at 123 is worth a note: the H2 4 expansion inserted 183 words *below* it rather than between the heading and the placeholder, so its "after H2 + 1 paragraph" placement survived the revision intact.

**§4.2c Table-redundant chart.** ✅ Pass. The two `remotion` placeholders are at 45 and 67; the draft's only markdown table runs 125–131, which is 80 and 58 lines away, far outside the ±20-line window. The one placeholder adjacent to the table (123) is `ai-prompt`, a scene rather than a chart, so 2c does not engage.

**§4.3 Off-shape markers.** Grepped for `\[Verify:` (lowercase v), `\[EXTERNAL_NEEDED:`, `TODO:`, `[TBD]`, `[IMAGE ` and `[VERIFY ` (missing colon). **Zero hits.** Every marker is in canonical Phase-4-greppable form.

Marker-shape problems (e.g., `[Verify:]` lowercase v, extra spaces, rogue formats the Phase 4 grep will miss):
- None.

## 5. Word count

- Draft: `2,732` words (body prose, from the intro through the end of the closing CTA, excluding frontmatter and excluding the `## FAQ` block; the four `[IMAGE:]` description lines are excluded as production specs rather than prose, they account for 153 of the 2,885 raw words in that range)
- Outline target: `2,530` words (outline §Word count roll-up "Total estimate (body prose, no FAQ)")
- Delta: `+8.0%`
- Band: `within ±10% = pass, no issue`

**No length issue is logged, and none should be.** §5 passes cleanly in the first of the three bands, so no trim/expand note is warranted and the word-count branch of the Step 4 verdict tree does not fire. Independently confirmed against the editor's own measurement, same basis, same figure.

Per-section breakdown against the outline's per-section estimates, for reference only, no issue is logged from any of these:

| Section | Outline estimate | Actual (image lines excluded) | Delta |
|---|---|---|---|
| Intro | 200 | 129 | -36% |
| H2 1 | 300 | 253 | -16% |
| H2 2 | 320 | 348 | +9% |
| H2 3 | 650 | 747 | +15% |
| H2 4 | 380 | 558 total, of which 165 is table markup → **393 prose** | +3% on prose |
| H2 5 | 330 | 314 | -5% |
| H2 6 | 250 | 296 | +18% |
| CTA | 100 | 87 | -13% |

Two observations the editor may want:

1. **The entire +177-word delta from draft-v1 (2,555 → 2,732) is the issue-1 expansion.** Lines 133–143 measure 183 words; every other diff hunk is a one-word or one-clause substitution with a net effect near zero. **The writer trimmed nothing elsewhere to compensate**, which is exactly what `review-v1.md` §10 instructed.
2. **H2 4's prose is now 393 against a 380 estimate, +3%.** Its draft-v1 problem was diagnosed as composition rather than length (9 prose sentences carrying a 375-word section, because the table absorbed the rest). That is now fixed on the composition axis without breaking the length axis: 18 prose sentences, a bullet list, and two new prose paragraphs, still within 3% of the planned section size.

## 6. Humanization floor (per `${CLAUDE_PLUGIN_ROOT}/standards/writing-standards.md` + `blog-ops/profile/voice.md`)

| Check | Pass? | Notes |
|---|---|---|
| No forbidden phrases (0 hits) | ✅ | Both lists grepped, zero hits. |
| Zero em-dashes (`—`, grep returns nothing) | ✅ | Zero. |
| En-dashes (`–`) only in numeric ranges | ✅ | Zero en-dashes present, check vacuously satisfied. |
| First-person throughout | ✅ | No third-person drift, including in the new content. |
| Active voice | ✅ | No passive-marker hits. |
| ≥1 rhythm marker in every H2 | ✅ | All eight H2s clear the "at least one of" bar, with more margin than draft-v1: every body H2 now carries a bullet list, three carry a standalone question, and all eight show sentence-length variation well above threshold. |
| ≥1 bullet list per ~300 words | ⚠️ (improved, no issue logged) | 7 bullet lists across 2,732 body words = 1 per ~390 words, against a floor of ~9 lists. Draft-v1 was 6 lists / 2,555 = 1 per ~426. **The specific defect review-v1 named is resolved:** H2 4 was the only body H2 with no bullet list, and it now has one (137–141, 5 items). Every body H2 now carries at least one. Closing the remaining gap would need two more lists in sections that do not want them, which is precisely the "filler lists" `review-v1.md` §7 issue 6 told the writer not to add ("Verify after applying issue 1 rather than adding filler lists elsewhere"). Verified as instructed; **no issue logged**, and re-logging it would be re-litigating a decision the editor already made. |
| ≥1 standalone-question paragraph per major section (if the selected author voice calls for it, per `blog-ops/profile/authors.md`) | ⚠️ (resolved as directed, no issue logged) | The voice does call for it (`voice.md` §Lexicon lists reader-directed rhetorical hooks as a signature). Body sections with one: **3 of 6**, up from 1 of 6 in draft-v1. H2 1 (35), H2 2 (43), H2 4 (133). H2 3, H2 5 and H2 6 have none; H2 5's three questions remain bullet items inside a checklist rather than standalone paragraphs. The strict floor is 6 of 6, but `review-v1.md` §9 item 5 explicitly capped this at two additions ("Two only, do not add one to every section") on the reasoning that "four rhetorical questions in a row is its own tell". The writer complied exactly. **No issue logged**: the shortfall is the editor's deliberate instruction, not writer drift, and the three sections without one each carry a bullet list plus a concrete number. |
| ≥1 concrete number or named example per major section | ✅ | H2 1: the five-step model, Microsoft named. H2 2: two cited studies, 2019. H2 3: 30+ tools, three months, 45→10 minutes, ten minutes. H2 4: NotebookLM, five criteria, a 75-minute recording, four months with a wearable. H2 5: "three things", "at least a dozen", @wojakcodes. H2 6: four checks, three or four businesses. |
| No em-dash overuse | ✅ | Zero em-dashes, so nothing to overuse. |
| Visible sentence-length variation in every section | ✅ | Per-section stddev 7.5 to 16.3, all far above the <4 fail threshold. See §1. |

## 7. Specific issues

Issues ranked by severity. `critical` = must fix before approve; `major` = fix if revising; `minor` = nit-pick.

**0 critical, 0 major, 2 minor.** Neither minor issue blocks the `approve` verdict per Step 4 (which gates only on 0 critical + 0 major). Both are one-line copyedits; see §10 for how they should be applied given that approval skips the writer loop.

| Section (H2) | Line | Severity | Issue | Fix instruction (verbatim for writer if revising) |
|---|---|---|---|---|
| Why you still take the notes, and AI does the rest | 49 | `minor` | **Sentence-initial lowercase letter, introduced by `review-v1.md` §9 item 4's own verbatim rewrite.** The line now reads "Then researchers re-ran it in 2019. [performance did not consistently differ between any of the groups](…), including a group that took no notes at all…". In draft-v1 the anchor sat mid-sentence after "Across the direct replications," so lowercase `performance` was correct. The §9 rewrite promoted the anchor to sentence-initial position but carried the lowercase `p` over from the old construction, so the published post would open a sentence with a lowercase letter. This is my predecessor's error, not the writer's: the writer applied the block exactly as it was given, which was the correct behavior. The anchor text is still the literal claim after capitalization, and outline §External link plan row 2's lowercase listing does not require preserving sentence-initial case. | Capitalize the first letter of the anchor text only: change `[performance did not consistently differ between any of the groups]` to `[Performance did not consistently differ between any of the groups]`. Change nothing else on the line: keep the URL, the rest of the anchor text, the "including a group that took no notes at all" clause, the meta-analysis clause, and the paragraph's position immediately after line 47. |
| Which AI note-taking tool fits which job | 141 | `minor` | **Near-duplicate forward-reference clause, introduced by this pass.** The fifth criteria bullet ends "Answer that before you record anyone, which is what the last section is for." Line 77, in H3 3.1, already reads "Recording is not always allowed, which is what the last section of this post is for." Two signposts to H2 6 built from almost the same eight words, 64 lines apart. Both signposts are individually useful and `review-v1.md` §9 item 1(a) explicitly asked the fifth criterion to "tie forward to H2 6", so the tie-forward itself is correct; only the phrasing collides. Low reader impact, and worth a one-clause variation rather than a rewrite. | Optional. Vary the wording of the newer one so the two signposts don't rhyme: change line 141's "Answer that before you record anyone, which is what the last section is for." to "Answer that before you record anyone. The last section of this post is how." Leave line 77 exactly as it is, and leave the bolded question "**Where does the audio actually go?**" unchanged. |

## 8. What the draft does well

Three specific strengths the writer should preserve on revision. Calling these out explicitly prevents the writer from rewriting strong sections accidentally.

- **The H2 4 expansion is a model of how to add shopping depth without becoming a roundup, and it must not be "improved" into one later.** Lines 133–143 close the exact gap review-v1 diagnosed (a category map with no criteria for choosing inside a category) using **zero** product names, zero rankings and zero numbers. The five criteria are each phrased as a question the reader can carry to any tool in any row, and four of the five are recycled from load-bearing claims the post already earned elsewhere rather than asserted fresh: "Does it hear the room, or only the call?" is the table's own failure column; "Does it chapter or timestamp long audio?" is H3 3.2's stats-lecture illustration; "Does it show the source passage behind a summary line?" is the exact capability H2 1's abstractive-summarization gloss explains the need for and that NotebookLM is named for at 145; "Where does the audio actually go?" ties forward to H2 6. That internal wiring is why the section now reads dense instead of padded, and it is also why the criteria list adds shopping utility without adding a single sourcing obligation. Do not add a sixth criterion, do not attach example tools to any bullet, and do not convert this list into a comparison table.
- **H2 2's retention passage (47–51) is still the best thing in the draft, and §9 item 4's rewrite made it better, not weaker.** It cites the original lecture-note finding, follows it in the very next paragraph with the 2019 replication and the meta-analysis *as two distinct findings*, names the flattening out loud ("That result traveled fast and got flattened into 'write it by hand'"), and then lands the only claim this blog is allowed to make: "Which tells you the pen was never the active ingredient… That choosing and rephrasing is where the understanding happens, and it is precisely the part a finished AI summary removes." That last sentence is the post's thesis earned from evidence, and the passage now tracks the published `digital-vs-paper-notes` correction almost clause for clause. Beyond the one-character capitalization fix in §7 issue 1, do not touch these three paragraphs, and never let a future pass reintroduce "handwriting helps you remember".
- **The H2 1 question-then-gloss restructure (35–39) is the writer exercising judgment correctly on an unflagged line, and it should stay.** Dropping the redundant clause and compressing the payoff to "That second kind is the culprit." avoided posing a question at 35 and answering it with the same eight words at 39, and it reordered the section into need → gloss → payoff, which teaches better than v1's gloss → payoff. It also preserves the `audience.md` §Reading posture requirement intact: `extractive` and `abstractive` still appear for the first time inside their own plain-English definitions at line 37, before any use, and the H2 5 callback at 155 ("that abstractive rewriting from earlier") still has its antecedent. This is the kind of small out-of-scope change a revising writer is normally penalized for; here it was necessary, minimal, and self-declared. Keep it.

Honorable mention, not to be lost: "a folder of summaries you never open is worse than three scrappy lines you wrote yourself, because it *feels* like preparation. Ouch." (line 157) remains the single most on-voice line in the post per `voice.md` §Lexicon. Also worth preserving: line 121's "I am not going to rank these" survived the expansion byte-identical, and the expansion is what finally makes that refusal credible rather than evasive.

## 9. Instructions for writer (only if verdict = `request_revisions`)

N/A. Verdict is `approve`; no writer revision pass is dispatched. The two `minor` items in §7 are editor-applied copyedits, not a revision brief. See §10.

## 10. Reviewer notes (free-form)

- **Verdict arithmetic.** No forcing condition from Step 4's `request_revisions` branch fired: zero §2 structural deltas (the H3 3.4 spelling change is the editor's own authorized normalization, recorded in §2 rather than failed), zero forbidden-phrase hits, zero unsourced numeric claims, word count at +8.0% (inside ±10%, first band, clean pass), zero forbidden-SERP external links, `modules.competitors` off so §3.6/§3.7 do not engage, and zero `[VERIFY:]` markers so §4.4 has nothing to fail. §7 carries 0 critical and 0 major. Step 4's terminal condition is met: all §s pass, 0 critical + 0 major → **`approve`**. I hold this call. Draft-v1's single blocking `major` was H2 4's thinness, and it is genuinely fixed, not papered over.
- **On the two `minor` issues and the fact that approval skips the writer loop.** Issue 1 (line 49's lowercase `performance`) is a real defect that must not ship: a published sentence starting with a lowercase letter is a visible typo, and it will be the first thing a reader's eye catches in the post's most important evidence passage. I deliberately did **not** rank it `major`. Ranking it `major` would force `request_revisions`, which at iteration 2 means a third writer pass and an editor escalation to the human, all to change one character that my own predecessor's verbatim fix block got wrong. That is disproportionate. **The editor should apply both §7 fixes directly to `draft-v2.md` before Stage 3d**, or carry issue 1 into `action-items.md` as a required pre-publish copyedit. Issue 2 is genuinely optional. If the editor would rather not hand-edit an approved draft, issue 1 alone is worth a targeted writer touch-up; issue 2 is not.
- **Why the "strongest free option" FAQ line at 200 is not a cannibalisation finding.** I checked it explicitly rather than skipping it, because it is the one superlative-shaped phrase in the post. Four reasons it clears: it is unchanged from draft-v1 and was reviewed clean at iteration 1, so escalating it now would be moving the goalposts between passes; the §10 bar I set was scoped to what the H2 4 *revision* introduced, and this is neither in H2 4 nor introduced; the FAQ question it answers ("What is the best free AI note-taking tool?") is verbatim from the approved, locked outline FAQ set and derives from a real PAA query, so the post has to answer it; and the answer does exactly what outline §FAQ block directed, naming the one free source-grounded option, stating plainly what it is not ("It is not a live meeting recorder, though"), and refusing to crown a winner ("Rather than hunting for one winner, match the tool to the job"). It nominates one hedged first-person option, it does not rank a field. Recording the reasoning so the editor can see it was tested, not missed.
- **Overlap between H2 4's criteria list (137–141) and H2 5's trial checklist (161–163), examined and cleared.** Both are question-shaped lists about evaluating a tool, roughly 20 lines apart, which is the most plausible way the expansion could have created redundancy. They are distinguished by framing and the draft states the distinction: H2 4's is pre-commitment ("Five things are worth checking before you commit"), H2 5's is post-trial ("Here's what I check on a trial run before a tool earns a place in the workflow"). Only one criterion genuinely echoes across them (navigability: "Does it chapter or timestamp long audio?" vs "Can I find a specific moment in it in under a minute?"), and even there the first is a spec question and the second is an outcome test on the reader's own recording. **No issue logged.** Flagging it for the record only, so that if a future pass ever merges or trims these sections, whoever does it knows the near-duplication was assessed and judged intentional.
- **External-link budget after Stage 3d, unchanged from draft-v1 and still the editor's call.** The draft renders 4 external links against `blog-craft.md` §External linking's 3–5 band. If Stage 3d resolves **both** `[EXTERNAL_LINK_NEEDED:]` markers, the post lands at 6, one over. Not a defect in this draft and no issue is logged, but the editor should decide at Stage 3d which of the two matters more, or accept 6 deliberately. My read is unchanged: the jurisdiction marker (174) is the more valuable of the two, since the post gives no legal advice and would benefit from pointing somewhere official; the accuracy marker (153) is the one the outline already pre-authorized to be dropped in favor of a qualitative claim, and its surrounding prose already stands without it.
- **Two Stage 3d items carried forward, both pre-existing.** (1) `support.google.com/notebooklm` at line 145 is `needs_verification` in `facts.md` §Named examples: confirm both the exact capability wording and, now, the free tier before this link ships, per outline §External link plan. §9 item 6 improved the framing to "Google offers it free" but attribution is not verification. (2) The `[EXTERNAL_LINK_NEEDED:]` at 153 should be resolved or the claim left qualitative; `facts.md` §Rejected names the 2020 PNAS ASR-disparities study as a candidate but forbids citing it until Stage 3d has actually loaded it.
- **Documentation nit for the archive.** `outline.md` line 155 still carries the British `recognise` in H3 3.4's heading while the draft now carries `recognize`, because `review-v1.md` §9 item 2 changed the draft without backfilling the outline. Harmless to the post, but it will read as unexplained drift to anyone auditing the archive later. One-word fix to `outline.md`, editor's discretion.
- **Autopilot posture.** Judged entirely on the artifact's merits. No issue in §7 asks for human intake or for a decision `brief.md` §Autopilot assumptions already recorded. The seven assumptions there (author, category, intent, research modules, product feature, no affiliate links, angle) remain faithfully reflected in draft-v2, and the revision disturbed none of them. `custom-instructions.md` §Content policy's affiliate-disclosure rule still does not engage: the draft carries zero Amazon links and zero tagged links of any kind, re-verified.
- **Explicitly not raised as a length issue,** per the editor's instruction and confirmed independently on my own measurement: 2,732 body-prose words against the outline roll-up of 2,530 is +8.0%, first band, clean §5 pass, no trim/expand note. The 32 words over the plan's 2,300–2,700 display band are the editor's accepted, deliberate call and are not a review finding.
