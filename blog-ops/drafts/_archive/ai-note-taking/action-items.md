# Action items: ai-note-taking

Compiled by: blog-post-workflow skill (editor role) at Stage 4b, from a grep of
`blog-ops/drafts/ai-note-taking/draft-v2.md` markers + `blog-ops/drafts/ai-note-taking/images.md`.

> **Path convention after Gate 2 finalize:**
> - **History / spec (read-only):** `blog-ops/drafts/_archive/ai-note-taking/`
> - **Live post (where you edit):** the WordPress draft, plus its canonical repo copy at
>   `content/blog/ai-note-taking.md`
> - **Asset folder:** `blog-ops/assets/ai-note-taking/`, contains `README.md` (copy of images.md)
>   plus the five rendered image files.

Read by: the human operator before publishing.

**Purpose:** the single page to work through after Gate 2. Estimated total time: **20 minutes**
(most of the usual work was done automatically: all five images are already rendered, both markers
were resolved at Stage 3d, and the four inbound links are applied at staging).

## 0. Pre-flight

- Draft (pre-finalize): `blog-ops/drafts/ai-note-taking/draft-v2.md` (humanized at Stage 3c,
  markers resolved at Stage 3d). Post-finalize: `content/blog/ai-note-taking.md` + the WordPress draft.
- Approved outline: `blog-ops/drafts/ai-note-taking/outline.md` → archived to
  `blog-ops/drafts/_archive/ai-note-taking/outline.md`
- Image plan: `blog-ops/drafts/ai-note-taking/images.md` → archived, and copied to
  `blog-ops/assets/ai-note-taking/README.md`
- Review history: `review.md` + `review-v1.md` → archived to `blog-ops/drafts/_archive/ai-note-taking/`
- Total word count: **3,279** for the whole file; **2,830** body prose (intro through CTA, excluding
  the FAQ and the `[IMAGE:]` lines), against the outline roll-up of 2,530.
- Author: `olga` (byline: Olga Pak)
- Post date used in frontmatter: `2026-09-09`. The console rewrites this to the real publish day on
  the branch immediately before merging, so do not hand-edit it.

**Two editor decisions from Stage 3d you should know about before you read the post** (full reasoning
in the archived `checklist.md` Notes):

1. **The post carries 6 external links, one over `blog-craft.md`'s 3-5 band.** Accepted deliberately.
   The two links added at Stage 3d (PNAS, FTC) back the post's two most contestable claims, and
   dropping either would leave a bare assertion in its honesty section.
2. **Body prose is 130 words above the plan's 2,700 ceiling** (2,830), because resolving the two
   markers added sourced content after the reviewer approved the draft. Not trimmed: the only way
   back inside the band was to cut reviewer-approved prose or one of the two new citations.

## 1. Create images (5 total)

Image spec: `blog-ops/drafts/_archive/ai-note-taking/images.md` (also at
`blog-ops/assets/ai-note-taking/README.md`). **All five were rendered automatically at Stage 4a.5
(5 rendered, 0 failed, 0 pending), so these are verify-the-render checks, not build TODOs.**
Estimated time: 5 minutes.

- [ ] Featured image: `featured.png`, see images.md §Featured image, type: `ai-prompt` — rendered.
      Ships as the WordPress `featured_media` via media upload, NOT as an in-post embed.
- [ ] Image 1: `ai-handles-you-handle.png`, see images.md §Image 1, type: `remotion` — rendered.
- [ ] Image 2: `five-step-workflow.png`, see images.md §Image 2, type: `remotion` — rendered.
      Verified by the editor to show the post's own five steps (capture, transcribe, summarize,
      organize, review), not the different five-step model that appears in `facts.md`.
- [ ] Image 3: `capture-situations.png`, see images.md §Image 3, type: `ai-prompt` — rendered.
- [ ] Image 4: `ask-before-recording.png`, see images.md §Image 4, type: `ai-prompt` — rendered.

**Remotion slots** (2 total): both built, registered and exported at Stage 4a.5. The composition
sources ship on the PR branch alongside the post.

- [ ] Remotion: `AiHandlesYouHandle` → `ai-handles-you-handle.png` (composition built, registered in
      `tools/remotion/src/Root.tsx`, exported)
- [ ] Remotion: `FiveStepWorkflow` → `five-step-workflow.png` (composition built, registered, exported)

**AI-generated slots** (3 total, `ai-prompt`): generated by the codex path at Stage 4a.5. Verify each
render looks right; none failed, so no `Prompt:` block needs re-running by hand.

- [ ] AI: `featured.png` (generated, reviewed, saved)
- [ ] AI: `capture-situations.png` (generated, reviewed, saved)
- [ ] AI: `ask-before-recording.png` (generated, reviewed, saved)

Saved to: `blog-ops/assets/ai-note-taking/`

## 2. `[VERIFY:]` markers , auto-resolved at Stage 3d

**None.** The writer emitted zero `[VERIFY:]` markers across both drafts. Nothing to do.

(`modules.competitors` is off for this blog, so the competitor-routed outcome cannot occur here.)

## 3. `[EXTERNAL_LINK_NEEDED:]` markers , auto-resolved at Stage 3d

Both were resolved with confirmed, allowlist-class sources. Nothing to do; this is the audit log.

| # | Line | Claim | Outcome | Source / note |
|---|---|---|---|---|
| 1 | `draft-v2.md:153` | speech-recognition accuracy varies with speaker accent, crosstalk and audio conditions | `resolved` | https://www.pnas.org/doi/10.1073/pnas.1915768117 (Koenecke et al., PNAS 2020). Abstract read in-browser and quoted verbatim: average word error rate 0.35 for black speakers vs 0.19 for white speakers, across five commercial systems. **Scoped in the prose**: the draft explicitly says the study measured one specific gap rather than accents in general, so the citation is not overclaimed. |
| 2 | `draft-v2.md:174` | recording-consent rules differ by jurisdiction | `resolved` | https://www.ftc.gov/business-guidance/resources/complying-telemarketing-sales-rule , the FTC's own sentence, confirmed live: "State laws vary on permitting the recording of telephone conversations and the requirements to obtain consent of the recorded party." Scoped to US state variation. The post names no states, quotes no statute, and gives no legal advice. |

**Third Stage 3d change, worth a glance when you read the post.** Verifying the NotebookLM help URL
revealed Google has **renamed the product to Gemini Notebook** (`support.google.com/notebooklm` now
redirects to `support.google.com/gemininotebook`). Two corrections followed:
- the post now says "Gemini Notebook, which Google used to call NotebookLM" and links the canonical
  help page `https://support.google.com/gemininotebook/answer/16164461` (HTTP 200);
- the flat "Google offers it free" claim became "a free tier with usage limits and a paid upgrade
  above it", because Google's own help centre carries both usage-limit and upgrade topics. The
  "100% free" framing had come from a tweet in `research/x.md`, not from Google.
- [ ] Sanity-check that naming reads right to you, since it is the only third-party tool the post names.

## 4. Fill `[INTERNAL_LINK_NEEDED:]` markers

**None.** The writer emitted zero. The post's five internal links were all planned in the outline and
are in place, root-relative with no trailing slash.

## 4b. Add inbound links from existing posts to this one

Four inbound links from the outline. These are **applied automatically at Stage 4b.5** to the repo
copies. Per the `wordpress-rest` adapter §4b, an edit to a repo markdown file does not by itself
reach the live site, so each one still needs applying to the live WP post — **except** that
`publish.wordpress.apply_inbound_links_live: true` is set for this blog, so once this post is
published the workflow applies them live via REST and records before/after in the checklist Notes.
Until this post is `publish` status, they stay repo-only.

- [ ] `content/blog/note-taking-methods.md`, anchor "using AI to take notes" → `/ai-note-taking`,
      in the section on choosing a method
- [ ] `content/blog/digital-vs-paper-notes.md`, anchor "AI note-taking" → `/ai-note-taking`,
      where the post discusses typing and speed of capture
- [ ] `content/blog/how-to-take-notes-on-ipad.md`, anchor "an AI note-taking workflow" →
      `/ai-note-taking`, in the apps/setup section
- [ ] `content/blog/cornell-note-taking-method.md`, anchor "AI note-taking" → `/ai-note-taking`,
      where the post covers reviewing and summarizing
- [ ] Confirm all four rendered correctly in the Gate 2 preview, and that each edited file is in the
      publish diff.

Hand-apply commands for any row that did not go live automatically:
```bash
# 1) fetch the target post's raw content
curl -sf -u "$WP_USER:${!WP_APP_PASSWORD_ENV}" \
  "$WP_BASE/wp-json/wp/v2/posts?slug=<existing-slug>&context=edit&_fields=id,content"
# 2) insert the anchor by hand into the returned content.raw at the section named above:
#    <a href="https://olgapak.com/ai-note-taking">"<anchor>"</a>
# 3) POST the edited content back (build the body with jq, never a hand-quoted string):
curl -sf -u "$WP_USER:${!WP_APP_PASSWORD_ENV}" -X POST \
  -H 'Content-Type: application/json' \
  "$WP_BASE/wp-json/wp/v2/posts/<id>" \
  -d @<(jq -n --arg c "<edited content.raw>" '{content:$c}')
```

## 5. Final manual read

- [ ] Read the whole draft aloud, catches AI tells the editor missed (awkward rhythm, overly formal
      connectors, "it's important to note that" variants)
- [ ] Grammarly score target: 80-85 (not 99, perfection is an AI signal). If over 95, deliberately
      loosen one or two sentences
- [ ] `modules.product` is on: scan for hallucinated product features against
      `blog-ops/profile/product.md`. The post makes exactly one product mention (the Text Summarizer,
      in workflow step 3) plus the closing CTA, and states no price or usage figure. Confirm nothing
      else crept in.
- [ ] `modules.competitors` is off, so there are no profile-backed competitor claims to verify. Do
      still check that the post makes no flat capability claim about a named third-party tool beyond
      what Google's own help page says about Gemini Notebook.
- [ ] Check the retention passage in "Why you still take the notes, and AI does the rest" still reads
      consistently with the published `digital-vs-paper-notes` post: the claim is about the
      processing, never that handwriting beats typing.

## 6. N/A for the WordPress adapter

WordPress has no author-map file to reconcile. Continue to §7.

## 7. Publish

- [ ] Open the WordPress draft preview: `<wp_preview_url>` (filled in at Stage 4b.5 staging; also in
      `pr-monitor.json`).
- [ ] Read it once more in the WP admin preview (title, excerpt, featured image already synced by the
      adapter).
- [ ] **Focus keyword.** `blog-ops/profile/site-conventions.md` §SEO plugin names **Rank Math**, and
      records that the focus keyword is **not settable via standard REST**. So set it yourself in the
      WP editor's Rank Math panel, to exactly: **`ai note taking`** (verbatim, not the title, not a
      paraphrase). The workflow never attempts this write.
- [ ] Click **Publish** in WP admin. This workflow never does that step for you.
- [ ] After publishing, verify the live post per §8.

## 8. Post-publish (within 24 hours)

- [ ] Verify the live URL: `https://olgapak.com/ai-note-taking` (no trailing slash,
      `blog.trailing_slash: false`)
- [ ] Submit URL to Google Search Console → URL Inspection → Request Indexing
- [ ] Submit URL to Bing IndexNow (via Bing Webmaster Tools or the CLI)
- [ ] Check featured image Open Graph preview:
      `https://www.opengraph.xyz/?url=https%3A%2F%2Folgapak.com%2Fai-note-taking`
- [ ] Check mobile rendering at a narrow viewport
- [ ] The FAQ ships as ordinary HTML; FAQPage JSON-LD is a Rank Math concern on this blog, not
      something the adapter emits. If Rank Math is set to emit it, validate at
      `https://search.google.com/test/rich-results?url=https%3A%2F%2Folgapak.com%2Fai-note-taking`
- [ ] Once live, confirm the four §4b inbound links were applied to the published posts (this blog
      has `apply_inbound_links_live: true`), and spot-check one of them in the browser.

## 9. Trigger Phase 5 repurpose (when ready)

`modules.repurpose` is **off** for this blog, so there is nothing to run here and the console's
`repurpose` chain link is dropped. If you turn the module on later, the command is:

```
/repurpose-blog-post ai-note-taking
```

## 10. Archive cleanup (automatic at Gate-2 finalize)

```bash
ls blog-ops/drafts/ai-note-taking/          # should NOT exist
ls blog-ops/drafts/_archive/ai-note-taking/ # should exist, contains all the working files
```

If the draft directory still exists, Gate 2 finalize didn't complete; re-run the finalize sequence.

---

## Marker grep summary (raw, for re-verification)

```
[VERIFY:]              , 0 hits
[EXTERNAL_LINK_NEEDED:], 0 hits (2 emitted by the writer, both resolved at Stage 3d)
[INTERNAL_LINK_NEEDED:], 0 hits
[IMAGE:]               , 4 hits (matches the 4 `### Image ` entries in images.md; total images = 4 + 1 featured = 5)
```

Grep command the editor ran at Stage 4b (against the pre-finalize draft):
```bash
grep -nE '\[(VERIFY|EXTERNAL_LINK_NEEDED|INTERNAL_LINK_NEEDED|IMAGE):' \
  blog-ops/drafts/ai-note-taking/draft-v2.md
```

Re-run against the live post before publishing:
```bash
POST=content/blog/ai-note-taking.md
grep -nE '\[(VERIFY|EXTERNAL_LINK_NEEDED|INTERNAL_LINK_NEEDED|IMAGE):' "$POST"
```

Expected output: zero hits. The four `[IMAGE:]` placeholders are replaced with real Markdown image
syntax by the adapter at Stage 4b.5 staging, pointing into `blog-ops/assets/ai-note-taking/`.

- [ ] All `[IMAGE:]` placeholders replaced with real Markdown image syntax and every path points to a
      file that exists in the asset folder
