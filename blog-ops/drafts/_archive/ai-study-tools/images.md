# Images: ai-study-tools

Written by: `suggest-images` skill (invoked by the editor at Stage 4a).
Sources: `blog-ops/drafts/ai-study-tools/outline.md` (image placement plan), `blog-ops/drafts/ai-study-tools/draft-v2.md` (`[IMAGE:]` placeholders), `blog-ops/drafts/ai-study-tools/facts.md` (chart/diagram data), `blog-ops/profile/image-style.md`, `blog-ops/profile/custom-instructions.md` §Image style, `${CLAUDE_PLUGIN_ROOT}/adapters/images/*.md` (per-type production specs).
Read by: the `image-builder` agent at Stage 4a.5 (renders `remotion` + `ai-prompt` slots) and the human (screenshot slots, failed fallbacks).

**Purpose:** every image slot in the finalized draft gets a concrete spec the human can execute without re-reading the post. One image slot = one detailed entry.

## Summary

- Total image count: `5` (1 featured + 4 in-post)
- Breakdown by type:
  - Remotion compositions: `3` (all in-post: 2 diagrams + 1 chart)
  - AI-generated (`ai-prompt`): `2` (1 featured + 1 in-post scene)
  - Screenshots: `0`
- File destination: `blog-ops/assets/ai-study-tools/`

## Featured image

> **Frontmatter only.** This asset is referenced from the post's cover-image frontmatter field per the publish adapter's convention (`adapters/publish/wordpress-rest.md`) and rendered as a banner above the title. Never duplicate it as the first in-post `[IMAGE:]` placeholder; the post would render the same image twice. Confirmed: no `[IMAGE:]` placeholder in `draft-v2.md` carries `featured.png` or the cover concept, so there is no duplication to fix.

- **Type:** `ai-prompt`, from `images.featured_default` in `blog-ops/config.yaml`, and locked by `blog-ops/profile/custom-instructions.md` §Image style, which forbids overriding the featured slot to `remotion` or anything else. A hand-lettered flat-vector hero is the expected deliverable, generated by codex at Stage 4a.5.
- **Dimensions:** 3:2 per `blog-ops/profile/image-style.md` §Aspect ratio defaults (target ~1536 x 1024). Keep the title band and the student's head inside the vertical safe zone so the 1.91:1 OG/Twitter crop clips neither.
- **Concept:** A student at a warm wooden desk, calm rather than overwhelmed, reaching toward one teal app tile that floats slightly closer and larger than the small arc of plain unlabeled tiles around her.
- **Archetype:** `scene-vignette`, a small illustrated moment showing the outcome the post argues for, not a diagram of it. It fits because the post's thesis is a decision a person makes, not a ranking: nine tools exist, you pick the one that matches the job in front of you. One person, one arc of options, one chosen tile reads in a thumbnail and needs no numerals fighting the lettering. The archetype governs composition only; palette, fonts, watermark, and the verbatim title band are unchanged from the house style.
  - **Ledger check.** Rotation history is the union of `blog-ops/featured-log/*.md` (5 entries) and the legacy read-only `blog-ops/featured-log.md` (5 rows), sorted by date ascending. No prior entry carries this slug, so nothing is being churned away from a re-run. The last two entries are `2026-09-02 screen-free-hobbies = negative-space` and `2026-08-26 bullet-journal-for-beginners = big-number`, so **`negative-space` and `big-number` are OFF LIMITS**.
  - **Alternatives considered and rejected:**
    - `pattern-break` (a grid of identical app tiles, exactly one teal), the closest rival and genuinely on-message. Rejected on two grounds: it was the archetype of both covers dated 2026-08-08 (`best-pens-for-note-taking`, `how-to-reduce-screen-time`), so the library is already heavy with it, and dropping the person turns a post about a student's decision into an abstract motif. The "one tile differs" idea survives inside the scene as the single accent element.
    - `object-metaphor` (one oversized app tile with nine faint tiles behind it). Rejected: it says "an app" rather than "your choice among apps", and it discards the recurring female character the house style asks for wherever a person fits.
    - `split-contrast` (vendor marketing page on one side, a student's real stack on the other). Rejected: that argument belongs to in-post Image 1, which renders it with real numbers; the cover would preview a body diagram.
    - `diagram-lite`. Rejected per the skill's own guidance: this concept needs no flow, and a card-and-arrow cover is the house default the rotation exists to break.
  - **Convergence guard (read before generating).** `2026-08-19 how-to-take-notes-on-ipad` was also a `scene-vignette` with a warm desk and the female character. Keep this one visibly distinct: a wider, airier framing with the arc of floating tiles as the second focal mass, a different ground hue (warm apricot, not the ipad cover's tones), the student reaching upward and outward rather than writing head-down, and no tablet or stylus anywhere in the frame.
- **Suggested filename:** `featured.png`
- **Alt text:** `A student at a warm desk reaching for one teal app tile among several plain floating options`

### Production spec

#### If `ai-prompt` (codex-automated; generated at Stage 4a.5)

- **Tool:** `codex / gpt-image via ${CLAUDE_PLUGIN_ROOT}/skills/generate-image-codex/SKILL.md (no API key; on failure the Prompt below is the manual paste-anywhere fallback)`
- **Prompt:**
  ```
  A friendly flat-vector editorial illustration on a single bright, saturated, warm
  background: a flat sunlit apricot orange field (peachy and warm, NOT marigold yellow,
  NOT terracotta red-brown, NOT sage green, NOT blue, NOT navy) filling the whole
  canvas, with generous empty space in the upper third for the title.

  Composition (scene-vignette archetype: one small warm illustrated moment, no panels,
  no cards, no charts, no infographic callouts, no numbered badges). A young woman with
  warm brown shoulder-length hair sits at a compact natural-oak desk in the lower right
  of the canvas, seen from a relaxed three-quarter angle, shoulders and upper body only,
  cropped at the desk edge. On the desk in front of her: an open notebook with a few
  suggestive pencil lines, no readable words, and a small terracotta pot with one sprig
  of greenery. Her expression is calm, curious and slightly amused, unhurried and
  clearly NOT anxious, overwhelmed or stressed. She leans back a little rather than
  hunching forward.

  Floating in the open air above and to her left, a loose gentle arc of five plain
  rounded-square app tiles, evenly spaced, drifting like paper cards, each about the
  size of her hand, each completely blank with no letters, no numbers, no logos and no
  symbols on it. Four of these tiles are drawn in soft cream white with a warm walnut
  brown outline, sitting further back, slightly smaller and quiet.

  THE ONE FOCAL DEVIATION: the fifth tile, nearest her raised hand and closest to the
  viewer, is drawn in soft teal (#0E9F8E), a touch larger than the others, tilted a few
  degrees, with a soft ground-free drop shadow under it so it clearly floats in front.
  Her hand is open and reaching toward that one teal tile, fingertips almost touching
  it, the natural reading being "she is picking this one". No other object competes:
  no laptop, no phone, no tablet, no stylus, no coffee mug, no stacks of books, no
  clutter, no other people, no floating icons beyond those five tiles.

  Title treatment overlaid across the upper left area, left-aligned, with clear space
  beneath it before the tiles begin, two lines, VERBATIM text:
  line 1, "9 Best AI Study Tools" in a playful hand-lettered brush script, with the
  single word "Best" in bright red (#E11D48) and "9", "AI", "Study" and "Tools" in
  near-black (#222222);
  line 2, "for Students in 2026, Ranked" in a bold rounded sans (Montserrat or
  similar), near-black (#222222), noticeably smaller than line 1, comma included.
  Small lowercase wordmark "olgapak.com" in the bottom-left corner, near-black at about
  55% opacity, unobtrusive.

  Palette: sunlit warm apricot ground, natural oak and warm walnut brown, cream paper
  white, a little muted green in the plant, near-black #222222 for outlines and title,
  one red accent word #E11D48, soft teal #0E9F8E used ONLY on the single chosen tile.
  Mood: clean, approachable, confident, optimistic, warm afternoon light. Flat vector,
  crisp shapes, subtle warm shading, no gradients beyond a hint of warm light, no
  outline-only line art.
  ```
- **Aspect ratio:** `3:2` (matches the featured-slot default in `blog-ops/profile/image-style.md` §Aspect ratio defaults)
- **Negative prompt** (if the tool supports it):
  ```
  no cold blue or navy background, no monochrome brand-blue scene, no sage green or
  marigold or terracotta ground, no anxious or stressed or exhausted expression, no
  head-in-hands pose, no tablet, no stylus, no iPad, no phone, no laptop, no coffee
  mug, no piles of books, no cluttered desk, no swarm or cloud of dozens of icons, no
  robot, no brain imagery, no glowing sci-fi hologram, no circuit-board motifs, no
  real brand logos or app icons (no ChatGPT, OpenAI, Google, Gemini, Notion, Khan
  Academy marks), no letters numbers or symbols drawn on the floating tiles, no
  numbered callouts or badges, no infographic panels, no cards with headings, no
  arrows, no charts, no extra people, no extra limbs, no malformed hands, no more than
  five floating tiles, no more than one teal tile, no garbled or misspelled lettering,
  no extra words beyond the two title lines and the wordmark, no lorem ipsum, no
  duplicated watermark, no 3D render, no stock-photo realism, no heavy drop shadows on
  text, no busy texture
  ```
- **Style reference (if any):** `blog-ops/profile/image-style.md` §Observed conventions (featured hero: hand-lettered script mixed with bold rounded sans, one red accent word, bright saturated single-color ground, recurring female character) and §Illustration palette (warm and inviting, brand blue never dominant). Prior covers for tonal family only, deliberately NOT for composition or hue: `blog-ops/assets/best-pens-for-note-taking/featured.png` (sage, `pattern-break`) and `blog-ops/assets/time-blocking/featured.png` (terracotta, `negative-space`).
- **Source data (if any):** none rendered as a figure. The five floating tiles are illustrative and deliberately do not count the post's nine picks; no numeral in the image states a count except the "9" inside the verbatim title, and the alt text claims no count.
- **Text-in-image warning:** this cover carries exact text (the post title verbatim plus the wordmark). Check the render word by word against the frontmatter `title:` value, `9 Best AI Study Tools for Students in 2026, Ranked`, including the numeral 9, the year 2026 and the comma before "Ranked". If the model garbles, misspells or reflows it, re-run once; if it still fails, keep the illustration and overlay the title as a post-processing text layer rather than shipping wrong lettering. The floating tiles must stay completely blank: any lettering the model invents on them is a defect, not a bonus.
- **Why this and not remotion/screenshot:** `images.featured_default: ai-prompt` plus `custom-instructions.md` §Image style lock the featured slot to the hand-lettered flat-vector hero. This is an illustration job, not a diagram job, and there is no screen to capture. Never convert this slot to `remotion`.

### Watermark

- **`remotion` slots:** use the shared `<BlogWatermark />` component (wordmark per `blog-ops/profile/image-style.md`; sizing, opacity and position per `${CLAUDE_PLUGIN_ROOT}/adapters/images/remotion.md`). Never inline a watermark, never reposition. In this project it renders bottom-left at `SAFE_MARGIN`, `bottom: 48`, 28 px, opacity 0.55, so every composition below keeps its content above y = 1050 to leave the required clear space.
- **Own-site/product UI screenshots:** not applicable, no screenshot slots in this post.
- **External screenshots:** not applicable, no screenshot slots in this post.
- **`ai-prompt` slots:** the featured prompt requests the `olgapak.com` wordmark inside the image, bottom-left, about 55% opacity per `image-style.md` §Watermark. If a render omits or garbles it, add it post-hoc rather than re-rolling the whole image. The in-post `ai-prompt` scene (Image 4) requests no lettering at all, so it carries no wordmark.

---

## In-post images

Each image below corresponds to an `[IMAGE: ...]` placeholder in `draft-v2.md`. One entry per placeholder, in draft order. The featured asset above is **not** an in-post entry.

**Placement rule:** in-post images sit *after* the section heading and the section's first body paragraph. All four placeholders in `draft-v2.md` already satisfy that rule as the writer placed them (lines 27, 106, 162, 184). Do not move them.

**Table-redundancy rule:** `draft-v2.md` contains no markdown tables at all, so no chart slot here duplicates one. Image 3 in particular is the only tabular rendering of the free-tier evidence: H2 6 deliberately carries that detail as prose.

### Image 1, after H2 "So What Are the Best AI Study Tools, Really?" + 1 paragraph

- **Draft placeholder (verbatim):** `[IMAGE: Proportional diagram of the first page of Google results for "ai study tools", showing 6 vendor homepages, 2 non-independent editorial roundups and 1 forum thread. Type: remotion. Suggested filename: serp-composition.png]`
- **Type:** `remotion`
- **Concept:** One horizontal bar split to true proportion across the nine page-one results, 6 vendor homepages, 2 editorial roundups and 1 forum discussion, with the three groups broken out beneath it as ranked pips and one line of plain evidence each.
- **Suggested filename:** `serp-composition.png`
- **Alt text:** `Page one for "ai study tools": 6 of 9 results are vendor homepages, 2 roundups, 1 forum thread`

#### Production spec

> Conform to `${CLAUDE_PLUGIN_ROOT}/adapters/images/remotion.md`. This is an **in-post** slot: NO title, NO subtitle. `<BlogWatermark />`, background and safe margin still apply.

- **Tool:** Remotion (React to PNG), project at `tools/remotion/`. Studio: `npx remotion studio --port=3003`.
- **Composition `<Still>` ID:** `SerpComposition` (register in `tools/remotion/src/Root.tsx` as `component={SerpComposition}`, the imported reference, never a JSX element)
- **Component file:** `tools/remotion/src/SerpComposition.tsx`
- **Canvas dimensions:** 1800 x 1200 (`CANVAS` from `src/theme.ts`, matches the series); render final at `--scale=2`.
- **In-post image: NO title, NO subtitle.** The diagram starts near the top safe margin and fills the canvas.
- **Visual spec (detailed):**
  - Title text: none (in-post). Subtitle text: none (in-post).
  - Fixed coordinate frame, every element positioned off a helper, never hand-placed: `LEFT_X = SAFE_MARGIN` (120), `RIGHT_X = CANVAS.width - SAFE_MARGIN` (1680), `BAR_W = 1560`, `SEG_GAP = 10`, `USABLE = BAR_W - 2 * SEG_GAP` (1540), `wAt(n) = (n / 9) * USABLE`, so the three segments are 1026.7 px (n=6), 342.2 px (n=2) and 171.1 px (n=1). Never round these by eye: the proportion is the claim.
  - Layout, top to bottom:
    1. **Eyebrow line, y = 170.** One line, left-aligned at `LEFT_X`, `fonts.sans` 30 px uppercase, letter-spacing 2, `palette.muted`: `WHAT PAGE ONE ACTUALLY IS`. This is a label, not a title band, and it stays a single quiet muted line.
    2. **Proportional bar, y = 230 to 350** (`BAR_H = 120`, `borderRadius: 16`, segments laid left to right with `SEG_GAP` between them). Segment 1 (n=6): `palette.surface` fill, `1px solid palette.border`. Segment 2 (n=2): `palette.warn` at 8% tint fill, `1.5px solid palette.warn`, this is a genuine caution state per §Color conventions, not decoration. Segment 3 (n=1): `palette.accent` at 10% tint fill, `2px solid palette.accent`, the composition's single accent, marking the only result on the page that is neither selling a product nor ranking itself.
       Inside each segment, vertically centered, one line: the count in `fonts.mono` 44 px, `palette.text` on segment 1, `palette.warn` on segment 2, `palette.accent` on segment 3. Segment 3 is 171 px wide, so its label is the bare `1`, and its full count string sits in its card below instead.
    3. **Three group cards, y = 430 to 900.** Equal widths, `COL_W = (BAR_W - 2 * 48) / 3` = 488, gutter 48, aligned to the bar's left edge. Standard card recipe from remotion.md §Card design language (surface, 1px border, radius 16, the shared card shadow, padding 26 by 28). Card 3 uses the emphasis variant (accent tint background, `2px solid palette.accent`); cards 1 and 2 stay neutral, with card 2's border tinted toward `palette.warn` per the warning variant. Each card holds, top to bottom: the group name in `fonts.sans` 40 px semibold `palette.text`; the count in `fonts.mono` 34 px (`palette.text` / `palette.warn` / `palette.accent` respectively); a row of rank pips; and a two-line evidence caption in `fonts.sans` 27 px `palette.muted`, wrapped inside the card, never overflowing.
    4. **Rank pips.** Small 52 x 52 rounded squares (radius 10), `1px solid palette.border`, `fonts.mono` 26 px `palette.text`, 12 px apart, wrapping to a second pip row inside card 1 if six pips exceed the card width. They carry the observed rank positions from `facts.md`, so the diagram is checkable against the SERP itself.
    5. **Footnote, y = 990 to 1040.** Left-aligned at `LEFT_X`, `fonts.sans` 26 px `palette.muted`, up to two lines. Everything ends above y = 1050 so the bottom-left watermark keeps its clear space.
  - Copy (exact strings, no em-dashes, no enclosed glyphs, no check or cross characters):
    - Eyebrow: `WHAT PAGE ONE ACTUALLY IS`
    - Bar labels: `6` / `2` / `1`
    - Card 1: name `Vendor homepages`, count `6 of 9`, pips `1` `2` `3` `4` `8` `9`, caption `The tools themselves. Two of them open by calling themselves the top platform in the category.`
    - Card 2: name `Editorial roundups`, count `2 of 9`, pips `5` `7`, caption `Neither is independent. One ranks its own product first with no disclosure, the other sells a $29 per month membership on the same page.`
    - Card 3: name `Forum discussion`, count `1 of 9`, pip `6`, caption `The only result on page one with nothing to sell you.`
    - Footnote: `Google page one for "ai study tools", observed 6 September 2026. The two roundups name seven and ten tools, overlap on two, and name none of the five vendors outranking them.`
  - Palette: per `blog-ops/profile/image-style.md` tokens plus remotion.md §Color conventions. Background `palette.background`; names and counts `palette.text`; captions, eyebrow, footnote and pip borders `palette.muted` / `palette.border`; `palette.accent` used once, on the forum group (segment 3 and card 3); `palette.warn` used once, on the roundup group, for a real caution state. No `opacity < 1` dimming anywhere.
  - Typography: `fonts.sans` for group names, captions, eyebrow and footnote; `fonts.mono` for every count, rank pip and the `$29` figure inside card 2's caption if it is split out. No em-dash (U+2014) in any rendered string.
  - Icons / SVG assets: none. `tools/remotion/public/` does not exist in this project, so draw everything in CSS and inline SVG; do not call `staticFile`.
  - Arrows / connectors: none. This is a proportion, not a flow. Do not add arrowheads between the bar and the cards; vertical alignment carries the relationship.
  - Card recipe: neutral variant for card 1, warning variant for card 2, emphasis variant for card 3, one emphasis card only.
- **Source data:** `facts.md` line 47 (6 of 9 results are vendor homepages, at ranks 1, 2, 3, 4, 8, 9, verified by direct observation), line 48 (2 editorial roundups at ranks 5 and 7, 1 Reddit discussion at rank 6, verified), line 49 (neither roundup is independent: one self-ranks at number one with no disclosure, the other promotes its own $29 per month membership, verified), line 50 (seven tools versus ten, overlapping on two) and line 51 (neither roundup names any of the five vendors outranking it). Nothing here is a vendor self-report; every number is a structural observation of the SERP. No hardcoded illustrative values.
- **Reference composition to mimic:** `tools/remotion/src/TwoPilesOfScreenTime.tsx` for its fixed coordinate frame, computed widths and caption discipline; `tools/remotion/src/SampleDiagram.tsx` for the base scaffold. Take the code conventions, not the two-panel layout.
- **Iteration command** (preview, throwaway, outputs to `tools/remotion/out/`, not committed):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still SerpComposition --output=out/preview-serp-composition-v1.png
  ```
- **Final export command** (only after sign-off; worktree-safe, writes into the CURRENT tree):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still SerpComposition --scale=2 \
    --output="$REPO/blog-ops/assets/ai-study-tools/serp-composition.png"
  ```
- **Why this and not a real screenshot:** a screenshot of the SERP would be stale within weeks, would render competitor branding at full size, and would not show the 6 / 2 / 1 proportion at all. Only Remotion renders the exact counts and true segment widths by construction, which is the whole point of the slot.

---

### Image 2, after H2 "Best AI Study Tools for Turning Material Into Recall Practice" + 1 paragraph

- **Draft placeholder (verbatim):** `[IMAGE: A loop diagram showing the recall cycle, material in, AI-generated questions out, self-testing, then identified gaps feeding back into the material. Type: remotion. Suggested filename: recall-loop.png]`
- **Type:** `remotion`
- **Concept:** A four-node clockwise loop, your material becomes AI-written questions, you answer them from memory, and the gaps you find go back into the material, with the answering step marked as the one that does the learning.
- **Suggested filename:** `recall-loop.png`
- **Alt text:** `Loop diagram: material becomes AI questions, you answer from memory, the gaps feed back in`

#### Production spec

> Conform to `${CLAUDE_PLUGIN_ROOT}/adapters/images/remotion.md`. This is an **in-post** slot: NO title, NO subtitle. `<BlogWatermark />`, background and safe margin still apply.

- **Tool:** Remotion (React to PNG), project at `tools/remotion/`. Studio: `npx remotion studio --port=3003`.
- **Composition `<Still>` ID:** `RecallLoop` (register in `tools/remotion/src/Root.tsx` as `component={RecallLoop}`, the imported reference, never a JSX element)
- **Component file:** `tools/remotion/src/RecallLoop.tsx`
- **Canvas dimensions:** 1800 x 1200 (`CANVAS` from `src/theme.ts`); render final at `--scale=2`.
- **In-post image: NO title, NO subtitle.** The ring is centered in the canvas and fills it.
- **Visual spec (detailed):**
  - Title text: none (in-post). Subtitle text: none (in-post).
  - Fixed coordinate frame: `CENTER = { x: CANVAS.width / 2, y: 620 }`, `R = 330`, `onRing(angleDeg)` returns `{ x: CENTER.x + R * cos, y: CENTER.y + R * sin }` exactly as `DoomscrollLoopDiagram.tsx` already does. Four nodes at -90 (top), 0 (right), 90 (bottom), 180 (left). `CARD_W = 400`, `CARD_H = 176`, each card centered on its ring point.
  - Layout:
    1. **Four cards** on the ring, standard card recipe (surface, `1px solid palette.border`, radius 16, shared shadow, padding 26 by 28). Each card holds a label in `fonts.sans` 40 px semibold `palette.text` and one caption line beneath in `fonts.sans` 26 px `palette.muted`, wrapped to at most two lines inside the card.
    2. **The bottom card is the emphasis variant**: `palette.accent` at 8% tint background, `2px solid palette.accent`, its label in `palette.text` and a small uppercase accent tag above the label in `fonts.sans` 22 px `palette.accent`, letter-spacing 2, reading `THIS IS THE PART THAT WORKS`. Exactly one emphasis card in the composition, per §Card design language.
    3. **Four connectors**, clockwise, top to right to bottom to left and back to top, drawn as SVG arcs or straight segments trimmed at both ends by a padding radius so no line touches a card (reuse the `arrow()` helper pattern from `DoomscrollLoopDiagram.tsx`). All four are identical sequential-step connectors per §Arrow conventions: `palette.muted`, `strokeWidth={2.5}`, solid, rounded caps, muted arrowhead marker. No accent path, the accent lives on the card, so the loop reads as continuous rather than as one favored branch.
    4. **Center label**, two lines centered at `CENTER`, max width 380: line 1 `fonts.sans` 30 px semibold uppercase `palette.muted`, letter-spacing 3; line 2 `fonts.sans` 26 px `palette.muted`. The center circle is roughly 230 px in radius of clear space, so keep it to these two short lines and never let it approach a card edge.
    5. **Footnote**, left-aligned at `SAFE_MARGIN`, y = 1000 to 1040, `fonts.sans` 26 px `palette.muted`, one line. Everything ends above y = 1050, clear of the watermark.
  - Copy (exact strings, no em-dashes, no enclosed glyphs):
    - Top card: label `Your material`, caption `A lecture, a reading, a set of slides`
    - Right card: label `AI writes the questions`, caption `Flashcards, a quiz, practice prompts`
    - Bottom card: tag `THIS IS THE PART THAT WORKS`, label `You answer from memory`, caption `Closed book, no scrolling back to check`
    - Left card: label `The gaps show up`, caption `Whatever you could not answer goes back in`
    - Center: line 1 `ACTIVE RECALL`, line 2 `Run the loop again at widening intervals`
    - Footnote: `Spaced repetition is the same loop, repeated at widening intervals rather than crammed once.`
  - Palette: background `palette.background`; card labels `palette.text`; captions, connectors, center label and footnote `palette.muted`; `palette.accent` used once, on the bottom card and its tag. `palette.warn` unused: nothing here is a failure state, and "the gaps show up" is a normal step of the loop, not an error. No `opacity < 1` dimming.
  - Typography: `fonts.sans` throughout; no numeric data in this diagram, so `fonts.mono` is not used. No em-dash in any rendered string.
  - Icons / SVG assets: none. Draw the connectors and arrowheads as inline SVG; `tools/remotion/public/` does not exist, so do not call `staticFile`.
  - Arrows / connectors: four sequential-step connectors as specified above, all identical in style and each trimmed by the same start and end padding so the ring is visually even.
  - Card recipe: neutral variant on three cards, emphasis variant on the bottom card only.
- **Source data:** conceptual diagram, no figures rendered. The vocabulary is grounded in `facts.md` line 111 (Mindgrasp's own page maps its features to Active Recall, Practice Testing, Spaced Repetition and Self-Explanation, verified as the vendor's framing, and the underlying techniques long predate AI) and matches the draft's own definitions of active recall and spaced repetition in this section. **The four-step loop shape itself is an editorial construction, a hardcoded illustrative structure, not a sourced figure.** No number appears in the image, so nothing here can contradict `facts.md`.
- **Reference composition to mimic:** `tools/remotion/src/DoomscrollLoopDiagram.tsx`, the existing ring-of-nodes composition in this project. Reuse its `onRing()` and `arrow()` helpers and its card sizing directly; drop from five nodes to four and from its warn-tinted final node to an accent-tinted emphasis node, since this loop is the good one rather than the trap.
- **Iteration command** (preview, throwaway, outputs to `tools/remotion/out/`, not committed):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still RecallLoop --output=out/preview-recall-loop-v1.png
  ```
- **Final export command** (only after sign-off; worktree-safe, writes into the CURRENT tree):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still RecallLoop --scale=2 \
    --output="$REPO/blog-ops/assets/ai-study-tools/recall-loop.png"
  ```
- **Why this and not a real screenshot:** the cycle spans several tools and several days, so no single screen shows it. A generated illustration would fumble the four short labels that carry the whole meaning; Remotion renders them exactly.

---

### Image 3, after H2 "The Paid All-in-Ones the Search Results Push (and What "Free" Means on Them)" + 1 paragraph

- **Draft placeholder (verbatim):** `[IMAGE: Simple chart of free-tier transparency across five vendor pages, 2 that state their free-plan limits versus 3 that do not. Type: remotion. Suggested filename: free-tier-transparency.png]`
- **Type:** `remotion`
- **Concept:** Five vendor pages split into two stacked blocks, the two that publish what their free plan actually includes (with the limit printed beside each) above the three that market "free" with no stated limit anywhere on the page.
- **Suggested filename:** `free-tier-transparency.png`
- **Alt text:** `Five AI study apps: two state their free-plan limits, three state no limit anywhere`

#### Production spec

> Conform to `${CLAUDE_PLUGIN_ROOT}/adapters/images/remotion.md`, including §In-post stacked comparisons. This is an **in-post** slot: NO title, NO subtitle. `<BlogWatermark />`, background and safe margin still apply.

- **Tool:** Remotion (React to PNG), project at `tools/remotion/`. Studio: `npx remotion studio --port=3003`.
- **Composition `<Still>` ID:** `FreeTierTransparency` (register in `tools/remotion/src/Root.tsx` as `component={FreeTierTransparency}`, the imported reference, never a JSX element)
- **Component file:** `tools/remotion/src/FreeTierTransparency.tsx`
- **Canvas dimensions:** 1800 x 1200 (`CANVAS` from `src/theme.ts`); render final at `--scale=2`.
- **In-post image: NO title, NO subtitle.** The comparison starts near the top safe margin and fills the canvas.
- **Visual spec (detailed):**
  - Title text: none (in-post). Subtitle text: none (in-post).
  - Fixed coordinate frame: `LEFT_X = SAFE_MARGIN` (120), `CONTENT_W = CANVAS.width - 2 * SAFE_MARGIN` (1560), rows full width, `rowY(i, top, h, gap) = top + i * (h + gap)`, never hand-placed.
  - Layout, top to bottom, a stacked comparison per §In-post stacked comparisons (stacked, not side by side, because each row needs the full width for its limit text):
    1. **Section tag A**, centered pill at y = 160 (height 56, generous horizontal padding, radius 28): `palette.accent` at 10% tint background, `palette.accent` text, `fonts.sans` 30 px bold uppercase, letter-spacing 2.
    2. **40 px gap**, then **two rows**, top = 230, `ROW_H = 140`, `ROW_GAP = 18` (so y = 230 to 528). Each row is a full-width card, emphasis variant (accent tint background at 6%, `2px solid palette.accent`, radius 16, shared shadow, padding 24 by 32). Row content, left to right on one baseline grid: the vendor name in `fonts.sans` 40 px semibold `palette.text` in a fixed 340 px column; then the stated limit, `fonts.sans` 30 px `palette.text` with every numeral in `fonts.mono` 30 px, wrapping to two lines inside the row rather than overflowing; then, right-aligned, a small badge (radius 8, `1.5px solid palette.accent`, accent text, `fonts.sans` 22 px bold uppercase, letter-spacing 1.5). Draw the badge as a styled pill with a word inside it, never a check-mark glyph.
    3. **Divider**, full width at y = 568, 1 px `palette.border`.
    4. **Section tag B**, centered pill with its vertical center at y = 612, same geometry as tag A but `palette.muted` at 10% tint background and `palette.muted` text.
    5. **40 px gap**, then **three rows**, top = 680, `ROW_H = 100`, `ROW_GAP = 14` (so y = 680 to 1008). Neutral variant cards (surface, `1px solid palette.border`). Same column grid: vendor name in `palette.text`, then the state text in `fonts.sans` 28 px `palette.muted`, then a right-aligned neutral badge (`1.5px solid palette.border`, `palette.muted` text). Convey "no limit stated" with muted color and an explicit label, never with `opacity < 1`.
    6. **Footnote**, left-aligned at `LEFT_X`, y = 1010 to 1046, `fonts.sans` 24 px `palette.muted`, one line, ending above y = 1050 so the watermark keeps its clear space.
  - Copy (exact strings, no em-dashes, no enclosed glyphs, no check or cross characters):
    - Section tag A: `2 OF 5 STATE THEIR FREE-PLAN LIMIT`
    - Row 1: `Studley` / `Free plan covers one study set, stated in its own FAQ` / badge `STATED`
    - Row 2: `Evernote` / `50 notes, 1 notebook, 5 spaces, 20 tags, 200 attachments, sync to 1 device, 1 GB storage` / badge `STATED`
    - Section tag B: `3 OF 5 STATE NO LIMIT ANYWHERE`
    - Row 3: `StudyFetch` / `Markets "free" with no stated limit, no reachable pricing page` / badge `NOT STATED`
    - Row 4: `Turbo AI` / `Markets "free" with no stated limit, no reachable pricing page` / badge `NOT STATED`
    - Row 5: `Mindgrasp` / `Markets "free" with no stated limit, no reachable pricing page` / badge `NOT STATED`
    - Footnote: `Five vendor pages fetched 6 September 2026. Free-plan terms change, so check the page before you upload a semester of work.`
  - Palette: background `palette.background`; vendor names and stated limits `palette.text`; the three opaque rows' state text, both badges on that side, the tag B pill and the footnote `palette.muted` / `palette.border`; `palette.accent` used for the transparent block only (tag A, the two row borders, their badges), which is this composition's single emphasis role. `palette.warn` deliberately unused: the post's own line is that an unstated free tier is a reason to check, not an accusation, and reserving warn keeps the accent semantics clean.
  - Typography: `fonts.sans` for names, state text, tags, badges and footnote; `fonts.mono` for every numeral in Evernote's and Studley's limits (`50`, `1`, `5`, `20`, `200`, `1 GB`), since those read as data. No em-dash in any rendered string. Note the deliberate use of straight double quotes around `"free"` in the three opaque rows and the footnote.
  - Icons / SVG assets: none, and **no vendor logos**. `tools/remotion/public/` does not exist; draw every badge and divider in CSS and inline SVG, do not call `staticFile`.
  - Arrows / connectors: none. This is a two-block comparison, not a flow.
  - Card recipe: emphasis variant on the two transparent rows (the one thing this image argues for), neutral variant on the three opaque rows.
- **Source data:** `facts.md` line 56 (of the five vendor pages fetched, only two state what their free tier includes; StudyFetch, Turbo AI and Mindgrasp market "free" with no stated limit and no reachable pricing page, verified by direct observation, the `pricingUrl` and `pricingText` fields empty for all three), line 57 (Studley's free plan is limited to one study set per its own FAQ, verified as stated on 2026-09-06) and line 58 (Evernote's free plan: 50 notes, 1 notebook, 5 spaces, 20 tags, 200 attachments, sync to 1 device, 1 GB storage, verified as stated on 2026-09-06). **Prices are deliberately excluded from the image** even though `facts.md` line 57 carries Studley's, because both fact rows flag that pricing changes; the post's prose carries the prices where a `[VERIFY:]` pass can reach them, and the image stays durable. No data is missing for this chart.
- **Reference composition to mimic:** `tools/remotion/src/TwoPilesOfScreenTime.tsx` (fixed frame, `rowY()` helper, full-width rows, section headings and captions) restructured from two side-by-side panels into the stacked two-block form; `tools/remotion/src/CostTierKey.tsx` for badge and pill treatment. Take the code conventions and the row grid, not the two-column split.
- **Iteration command** (preview, throwaway, outputs to `tools/remotion/out/`, not committed):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still FreeTierTransparency --output=out/preview-free-tier-transparency-v1.png
  ```
- **Final export command** (only after sign-off; worktree-safe, writes into the CURRENT tree):
  ```
  REPO=$(git rev-parse --show-toplevel) && cd "$REPO/tools/remotion" && npx remotion still FreeTierTransparency --scale=2 \
    --output="$REPO/blog-ops/assets/ai-study-tools/free-tier-transparency.png"
  ```
- **Why this and not a real screenshot:** the finding is an absence spread across five separate pages, and an absence cannot be screenshotted. Five stitched captures would also age badly and reproduce five vendor brand marks at full size. The slot's whole point is verbatim numerals (`50 notes`, `1 GB`, `2 of 5`, `3 of 5`), which is a Remotion job by construction.

---

### Image 4, after H2 "When AI Is the Wrong Tool for Studying" + 1 paragraph

- **Draft placeholder (verbatim):** `[IMAGE: A warm, quiet scene of a closed laptop pushed to one side of a desk with a hand-worked problem set and a pencil in front of it. Type: ai-prompt. Suggested filename: closed-laptop.png]`
- **Type:** `ai-prompt`
- **Concept:** A quiet warm desk seen at a slight angle, a closed laptop pushed off to one side and out of reach, with a half-worked problem set in pencil and a well-used eraser holding the center of the frame.
- **Suggested filename:** `closed-laptop.png`
- **Alt text:** `A closed laptop pushed aside on a warm desk beside a problem set worked out by hand in pencil`

#### Production spec

#### If `ai-prompt` (codex-automated; generated at Stage 4a.5)

- **Tool:** `codex / gpt-image via ${CLAUDE_PLUGIN_ROOT}/skills/generate-image-codex/SKILL.md (no API key; on failure the Prompt below is the manual paste-anywhere fallback)`
- **Prompt:**
  ```
  A warm, inviting flat-vector illustration, no title lettering, no headline, no
  wordmark: a quiet desk scene viewed from above at a slight three-quarter angle, warm
  late-afternoon light falling across it from the upper left, with soft long shadows.

  The desk is natural light oak with visible warm wood grain, filling the frame, with
  generous empty wood space around the objects so the scene breathes.

  Foreground, holding the center of the frame: a single sheet of cream ruled paper
  lying slightly askew, covered in a half-finished handwritten working, loose pencil
  cursive and a few simple pencil-drawn diagrams, with one line crossed out and
  rewritten below it. The handwriting is suggestive scribble, not readable, no legible
  words, no numbers, no formulas, no labels. A well-used yellow-brown wooden pencil
  with visible sharpening marks rests diagonally across the lower right corner of the
  page, and a small worn white eraser with faint grey smudges sits beside it, with a
  few crumbs of eraser dust on the wood.

  Background, pushed to the upper left corner and clearly out of reach: a closed
  laptop, plain matte warm grey with a rounded profile, lid fully shut, blank and
  unlit, angled away from the viewer and partly cropped by the edge of the frame, with
  a soft shadow beneath it. It is quiet and inert, not broken, not slammed shut, and
  it carries no logo, no glowing light, no sticker.

  One small terracotta pot with a sprig of green leaves sits at the far edge for
  warmth. Nothing else in the frame: no phone, no tablet, no mug, no lamp, no books,
  no person, no hands.

  Palette: natural warm oak and walnut brown, cream paper, soft graphite grey for the
  pencil marks and the laptop, near-black #222222 outlines, warm sunlight, one small
  patch of muted green from the plant, and a single small teal (#0E9F8E) detail on the
  pencil's ferrule as the only cool accent. Mood: calm, focused, unhurried, the quiet
  after closing the laptop. Flat vector, crisp shapes, subtle warm shading, no
  gradients beyond the warm light, no outline-only line art.
  ```
- **Aspect ratio:** `3:2` (matches `blog-ops/profile/image-style.md` §Aspect ratio defaults, keeps this scene consistent with the featured hero and the three 3:2 Remotion diagrams)
- **Negative prompt** (if the tool supports it):
  ```
  no cold blue or navy scene, no monochrome brand-blue illustration, no dominant blue
  objects, no open laptop, no lit or glowing screen, no visible screen content, no
  phone, no tablet, no stylus, no coffee mug, no lamp, no stack of books, no person,
  no hands, no fingers, no title text, no headline, no lettering of any kind, no
  legible words, no numbers, no mathematical formulas, no labels, no captions, no
  watermark, no logos or brand marks on the laptop, no numbered callouts or badges, no
  arrows, no infographic panels, no cards, no charts, no red X or prohibition symbol
  over the laptop, no cracked or damaged laptop, no clutter, no 3D render, no
  stock-photo realism, no heavy drop shadows on objects, no busy texture
  ```
- **Style reference (if any):** `blog-ops/profile/image-style.md` §Illustration palette, explicitly the warm wood-toned `finished-notebooks-shelf` reference look. Avoid the cold, too-blue feel of `student-desk-spiral` and `rocketbook-workflow`, which that section names as the mistake to steer away from. Section illustrations must not be monochrome brand blue.
- **Source data (if any):** none. This is a conceptual scene supporting the section's argument, not a figure. It renders no count and no claim, and the section's one statistic (57% of U.S. college students using AI weekly, `facts.md`) is deliberately left in the prose rather than drawn.
- **Text-in-image warning:** this slot needs **no text at all**. The prompt asks for unreadable pencil scribble specifically so the model has nothing exact to garble. Per `image-style.md` §Observed conventions some section illustrations carry a lettered subheading, but that convention is skipped here (as it was on the previous post's in-post scenes) because garbled lettering is the most common failure mode and this scene reads perfectly without it. If the model writes any words, numbers or formulas on the page, re-run rather than shipping them.
- **Why this and not remotion/screenshot:** it is a mood and a moment, an argument for closing the laptop, with no data, no flow and no structure to render. `custom-instructions.md` §Image style reserves `remotion` for genuine diagrams and charts and points scenes and conceptual visuals at `ai-prompt`. There is nothing real to screenshot.

---

## File destination (copy these paths when saving)

```
blog-ops/assets/ai-study-tools/
├── featured.png                  (ai-prompt, codex)
├── serp-composition.png          (remotion, SerpComposition)
├── recall-loop.png               (remotion, RecallLoop)
├── free-tier-transparency.png    (remotion, FreeTierTransparency)
└── closed-laptop.png             (ai-prompt, codex)
```

Asset folder is created automatically by the Phase 4 image-generation step; this skill does not create it.

## Tools reference

1. **Remotion compositions:** project at `tools/remotion/`. Adapter: `${CLAUDE_PLUGIN_ROOT}/adapters/images/remotion.md`. Studio: `npx remotion studio --port=3003`. Register a `<Still>` in `src/Root.tsx` (component reference, never a JSX element), write the component in `src/<Id>.tsx`, iterate via `npx remotion still <Id> --output=out/preview-...png` (throwaway), final export via `npx remotion still <Id> --scale=2 --output="$REPO/blog-ops/assets/ai-study-tools/<filename>"`. Use `<BlogWatermark />` and the tokens in `src/theme.ts`, never inline a hex value or a font stack.
2. **AI-generated (`ai-prompt`, automated via codex, no API key):** adapter `${CLAUDE_PLUGIN_ROOT}/adapters/images/ai-prompt.md` plus `codex.md` for dispatch. On failure, the `Prompt:` blocks above are the verbatim manual fallback.
3. **Screenshots:** none in this post.
4. **Watermark and polish:** `remotion` slots use the shared `<BlogWatermark />`. The featured `ai-prompt` slot requests the `olgapak.com` wordmark inside the prompt; the in-post `ai-prompt` scene carries none by design. Output format: PNG for every slot.

## Naming conventions (from `${CLAUDE_PLUGIN_ROOT}/standards/blog-craft.md`)

- Lowercase, kebab-case, descriptive
- Good: `serp-composition.png`, `free-tier-transparency.png`
- Bad: `image1.png`, `screenshot.jpg`, `IMG_0042.PNG`

## What the human does

1. Normally nothing. All five slots are auto-dispatchable at Stage 4a.5 (3 `remotion`, 2 `ai-prompt`), and there are no screenshot slots to capture.
2. If a slot is recorded `failed`, copy its `Prompt:` block verbatim into any AI image tool, or fix and re-render the composition, then save to `blog-ops/assets/ai-study-tools/` under the exact suggested filename.
3. Verify file sizes (under 200 KB for featured, under 500 KB for in-post is a good rule of thumb).
4. Once all images exist locally, tick the "Create images" section in `action-items.md`.

## Editor notes

- **Counts match.** `draft-v2.md` contains exactly 4 `[IMAGE:]` placeholders (lines 27, 106, 162, 184) and `outline.md` §Image placement plan lists exactly 4 in-post slots plus the frontmatter-only featured slot. No delta.
- **No placeholder disagrees with the outline.** Each draft placeholder matches its outline slot in concept, type and filename. The only wording difference is cosmetic: the outline says "2 compromised roundups", the draft says "2 non-independent editorial roundups". Both describe `facts.md` line 49, and the spec follows the draft's wording.
- **No featured duplication.** No `[IMAGE:]` placeholder carries `featured.png` or the cover's concept, so the hygiene rule is satisfied with nothing to flag.
- **No table-duplicate chart.** `draft-v2.md` contains no markdown tables anywhere, so Image 3 is the only structured rendering of the free-tier evidence, exactly as the outline intended.
- **No chart needs fresh data.** Every figure in Images 1 and 3 comes from `facts.md` §Verified (lines 47 to 51 and 56 to 58), all marked verified by direct observation on 2026-09-06. Image 2 renders no figures and Image 4 renders none either. Nothing is blocked on a data pull.
- **Judgment call, Image 1's accent.** The single accent goes to the forum-discussion result (rank 6), on the reading that it is the only page-one result with nothing to sell. That is an editorial emphasis, not a claim in `facts.md`. If the human would rather the diagram stay neutral, drop the accent tint on segment 3 and card 3 and make all three groups neutral, everything else in the spec still holds.
- **Judgment call, anonymity in Image 1.** The two roundups are described but not named in the image, mirroring the draft, which describes them without naming them. `facts.md` names both (RemNote at rank 5, The Rundown AI at rank 7) if the human prefers to name them, but then the draft prose should name them too, or the image and the text will disagree in tone.
- **Judgment call, no prices in Image 3.** Studley's `$3.74 / $1.88 per week` is verified in `facts.md` line 57 and appears in the prose, but it is excluded from the rendered image because both free-tier fact rows warn that pricing changes. A price baked into a PNG cannot be corrected by a `[VERIFY:]` pass later.
- **Featured archetype churn risk.** `scene-vignette` was also used on 2026-08-19 (`how-to-take-notes-on-ipad`), which is outside the two-post block window but close enough to converge. The §Convergence guard above lists the specific differences to hold to. If the human would rather rotate further away, `pattern-break` is the strongest substitute and the prompt's floating-tile arc converts to it cleanly, at the cost of dropping the character.
- **Type ladder.** `remotion` is enabled and takes all three diagram/chart slots. The two `ai-prompt` slots are not fallbacks: the featured slot is locked to `ai-prompt` by config and `custom-instructions.md`, and Image 4 is a warm scene with no data, which `custom-instructions.md` §Image style explicitly routes to `ai-prompt`. No slot was blanket-converted, and no slot uses a type outside `images.enabled`.
